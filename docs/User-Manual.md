# ApexHMI 操作说明

> 本说明覆盖两大核心功能：**程序生成**（手动 / 自动 / 初始化）与**画布设计**。
> 适用版本：v0.8 及以后（含 M1-M4 + B1-B3 改造）。

---

## 目录

1. [程序生成 — 总览](#一程序生成--总览)
   - [1.1 手动程序生成](#11-手动程序生成)
   - [1.2 自动程序生成（SFC 步骤式）](#12-自动程序生成sfc-步骤式)
   - [1.3 初始化程序生成](#13-初始化程序生成)
   - [1.4 Git 代码同步](#14-git-代码同步)
2. [画布设计 — 总览](#二画布设计--总览)
   - [2.1 工作区结构](#21-工作区结构)
   - [2.2 控件工具箱](#22-控件工具箱)
   - [2.3 拖拽与定位](#23-拖拽与定位)
   - [2.4 属性编辑器](#24-属性编辑器)
   - [2.5 数据绑定](#25-数据绑定)
   - [2.6 事件与动作](#26-事件与动作)
   - [2.7 动画系统](#27-动画系统)
   - [2.8 多语言文本](#28-多语言文本)
   - [2.9 Faceplate（自定义复合控件）](#29-faceplate自定义复合控件)
   - [2.10 全局样式 / 资源](#210-全局样式--资源)
   - [2.11 项目导出 / 导入](#211-项目导出--导入)
   - [2.12 离线模拟](#212-离线模拟)
3. [快捷键速查](#三快捷键速查)
4. [常见问题](#四常见问题)

---

## 一、程序生成 — 总览

软件主导航「**设计器**」分组下有三个子页：

| 子页 | 用途 | 输出 |
|---|---|---|
| 手动程序生成 | 基于 IO 表，按命名规则一键生成 PLC `*.st` 源代码 | DB IO 块 / DI-DO Mirror / ACT_Comment |
| 自动程序生成 | 基于 SFC 步骤集合，生成自动流程 PLC 代码 | OPXX_Graph 自动主程序 |
| 初始化程序生成 | 与自动程序对称，独立步骤集合，生成初始化流程 | OPXX_Graph 初始化主程序 |

> 三个子页右上角带有「**手动程序生成 / 自动程序生成 / 初始化程序生成**」横向切换按钮，互相切换不丢未保存数据。

---

### 1.1 手动程序生成

#### 操作流程

1. **PLC 模板**：左栏下拉选择目标 PLC 型号（默认"汇川中型 PLC"）
2. **工位号 / OP 编号**：填工位号，如 `OP10`、`OP70`
3. **保存间隔（分钟）**：小于 5 分钟时，重复保存会写入同一文件并追加履历
4. **导入 IO 表**：点击「导入 IO 表」按钮选 Excel / CSV 文件
   - 自动解析输入地址 / 输入注释 / 输出地址 / 输出注释 4 列
   - 解析后在「IO 表预览」DataGrid 中显示
5. **校验与生成**：点「**生成程序**」按钮
   - **D1 自动校验**：地址重复 / 类型不匹配的行会被高亮，生成被阻止
   - 校验通过后生成 PLC 代码，落到 `<PLC 程序保存目录>/2.Main/Main_IO.ST` 等约定路径
6. **预览结果**：右栏「程序预览」面板：
   - 文件下拉选择刚生成的具体 `.st` 文件
   - 「**对比 Git**」（D2）：弹出 side-by-side 对比窗，红/绿色标差异
   - 「**复制当前文件**」：复制内容到剪贴板
   - 「**打开当前文件**」：用系统默认编辑器打开

#### 多工位批量生成（D3）

左栏底部「**多工位批量生成**」卡片：
- 输入 CSV 工位号清单，如 `OP30,OP40,OP50`
- 点「批量生成」逐个切换 OP 后跑生成
- 适合一次性给整条产线所有工位生成代码

---

### 1.2 自动程序生成（SFC 步骤式）

按"步骤 → 动作 → 跳转"组织的 SFC 流程，生成 OPXX_Graph 自动主程序。

#### 操作流程

1. **左栏选择工位号**（与手动程序的 OP 编号联动）
2. **添加步骤**：中栏「SFC 步骤列表」DataGrid
   - 「+ 添加步骤」按钮新增一步
   - 每步可填：StepNo / 动作清单 / 跳转条件
3. **编辑动作（每步的右侧 ActionSummary）**：
   - 双击 ActionSummary 单元格 → 弹「动作编辑」对话框
   - 支持的动作类型：气缸、轴、机械手、挡停、电机、报警、延时、条件分支
   - 每条动作配 DeviceName + ActionType + 可选参数
4. **编辑跳转（NextSummary）**：
   - 默认顺序跳到下一步
   - 可改为条件跳转：`Step5` 或 `IF DB1050.X.Done THEN Step3 ELSE Step8`
5. **生成代码**：右栏「**SFC 程序预览**」按钮
   - 自动按 SFC 模板渲染 ST 代码
   - 落到 `OPXX_Graph_ACT_AutoRunST<N>.st` 文件
6. **代码差异对比**：右栏顶部行点击拖动 → 显示「**对比 Git**」按钮

#### 报警术语本地化

中栏顶部「**编辑中文→英文报警术语对照表**」按钮：
- 弹「报警术语词典」对话框
- 中文报警 → 英文别名映射
- 生成代码时自动按映射翻译注释

---

### 1.3 初始化程序生成

与「自动程序生成」结构对称，独立步骤集合，生成 **OPXX_Graph 初始化主程序**。

操作与 §1.2 完全相同，唯一区别：
- 输出文件名是 `OPXX_Graph_ACT_InitST<N>.st`（非 AutoRunST）
- 步骤集合独立维护，与自动程序互不影响

> 初始化流程常用于：上电自检、回原点、参数加载、连接外设。

---

### 1.4 Git 代码同步

在「手动程序生成」左栏的「**Git 代码拉取**」Expander 内：

#### 配置项

| 字段 | 说明 |
|---|---|
| **仓库地址 (Repository URL)** | 如 `https://github.com/your/repo.git` |
| **本地目录 (Target Folder)** | 拉到本地的目录。首次拉取时若目录非空且非 Git 仓库将报错 |
| **项目子文件夹** | 留空则直接拉到保存目录；填写则作为子目录，并 checkout 到同名本地分支 |
| **分支 (Branch)** | 留空使用默认分支（master/main） |
| **HTTP 代理** | 如 `http://proxy.local:8080`，留空不启用 |
| **认证（可选 · 私有仓库）** | 展开后填用户名 / 密码 / token |

#### 工作流选项

| 选项 | 默认 | 说明 |
|---|---|---|
| 生成程序后自动追加到 Git 目录对应 `.st` 文件 | ✅ | 按映射规则把 `DBXX_IO` → `2.Main/Main_IO.ST` 等 |
| 拉取 `.project` 工程文件 | ❌ | 默认不拉取，避免覆盖现场工程文件 |
| 强制覆盖本地 (`reset --hard` + `clean -fd`) | ❌ | 勾选后丢弃所有本地未提交改动 |
| 拉取后推送新分支到远端 (`git push -u`) | ❌ | 本地项目分支没有上游时自动 push |
| 生成程序后自动 commit + push 到远端 | ❌ | 每次生成后自动 `add -A → commit → push` |
| 自动 commit message 模板 | `HMI 生成程序 ({op}) @ {time}` | `{op}` 替换工位号、`{time}` 替换时间戳 |

#### 操作按钮

- **拉取 / 更新**：从远端 fetch + checkout 到本地分支
- **打开本地目录**：用资源管理器打开本地仓库目录

---

## 二、画布设计 — 总览

主导航「**设计器 → 画布设计**」子页。这是构建 HMI 运行时画面的核心工作区。

### 2.1 工作区结构

```
┌─────────────────────────────────────────────────────────┐
│  顶部：[工具箱] (左) │ [动作按钮组] (右)                  │
├──────────┬─────────────────────────────────┬────────────┤
│ 左栏      │                                 │  右栏       │
│ 工程信息   │      中央画布                    │ 属性编辑器   │
│ 页面树    │  (拖拽控件、缩放、网格、对齐)      │ (4 Tab)     │
│ 库 (Lib)  │                                 │            │
│ 资源      │                                 │            │
└──────────┴─────────────────────────────────┴────────────┘
```

#### 顶部按钮组

| 按钮 | 功能 |
|---|---|
| 🗄 **PLC 变量** | 从 PLC 程序保存目录加载 `Device.Application.xml`，按 OP 工位号筛选导入变量表 |
| 📁 文件夹 | 手动选择 PLC 变量 XML 文件 |
| 🎨 **样式** | 全局样式编辑器（色板 + 字体预设） |
| 🔖 收藏 | 把当前选中控件存到项目库 |
| 🌍 全局库 | 把当前选中控件存到全局库（跨工程共享） |
| 🌐 **文本** | 多语言文本资源编辑 |
| 🌐 zh-CN | 当前语言切换下拉 |
| 📋 **列表** | 文本列表 / 图形列表资源编辑 |
| 🧩 **Faceplate** | 切换到 Faceplate 编辑模式 |
| 📦 **导出** | 工程打包为 zip |
| 📥 **导入** | 从 zip 导入工程 |
| 〰 **模拟** | 离线模拟开关（注入 SimTag 假数据） |
| 🚀 **发布** | 保存工程并推送到运行时 |

---

### 2.2 控件工具箱

工具箱按 WinCC 风格分 5 大组：

| 组 | 包含控件 |
|---|---|
| **基本对象** | 文本 / 矩形 / 椭圆 / 直线 / 折线 / 多边形 / 图形视图 / 数字 I/O / 符号 I/O / 图形 I/O / 日期时间 |
| **元素** | 按钮 / 开关 / 圆形按钮 / 棒图 / 量规 / 滑块 / 滚动条 / 时钟 / 组合框 / 列表框 / 复选框 / 单选 |
| **控件** | 趋势视图 / 报警视图 / 表格视图 / 画面窗口 |
| **媒体/分析** | HTML 浏览器 / PDF 视图 / 媒体播放器 / XY 趋势 / 报表视图 |
| **业务** | 配方视图 / 用户视图 / 系统诊断 / 报警指示器 / 状态强制 |
| **我的 Faceplate** | 内置气缸 / 内置轴 / 内置机械手 / 内置挡停 + 用户自定义 |

#### 工具栏 📌 悬浮 / 固定

工具箱右上角有「📌 PinOutline / Pin」图钉按钮：
- **固定模式**（默认）：工具箱在顶部 Row 0
- **悬浮模式**：工具箱浮起来可拖动（Thumb 拖把柄），允许移到画布上方任何位置

---

### 2.3 拖拽与定位

#### 基础操作

- **拖控件到画布**：从工具箱按住左键拖到画布释放，自动添加到鼠标释放点
- **拖变量到画布**：从左栏「PLC 变量」树拖变量到画布
  - 弹候选菜单（按变量类型）：
    - BOOL → 按钮 / 指示灯 / 开关 / 复选框
    - INT/REAL → I/O 域 / 棒图 / 量规 / 滑块 / 数值显示
    - STRING → 文本域 / 符号 I/O 域
  - 选定后自动创建控件 + 完成变量绑定
- **选中**：左键点击控件
- **多选**：Ctrl + 点击、或鼠标空白处拖框选
- **移动**：选中后拖动
- **调整大小**：选中后边缘出现 8 个手柄，拖手柄

#### 画布工具

| 工具 | 位置 | 功能 |
|---|---|---|
| **网格** | 画布顶部 | ☑ 显示网格（间距可调） |
| **吸附** | 同上 | ☑ 自动吸附到网格 |
| **缩放** | 同上下拉 | 50% / 75% / 100% / 125% / 150% / 200% |
| **适应窗口** | 缩放右侧 | 自动按画布尺寸缩放；再次点击恢复 100% |
| 鼠标坐标 | 同上 | 实时显示当前鼠标在画布坐标 |
| 控件数 | 同上 | 当前画布上 widget 总数 |

#### 多选对齐

选中 2+ 控件后画布顶部出现对齐工具栏：
- **左对齐 / 右对齐 / 顶对齐 / 底对齐**
- **横向均布 / 纵向均布**：3+ 控件时按等间距
- **等宽 / 等高**：以选中第一个为基准
- **删除**：批量删除选中

#### 双击画布弹出独立窗口

双击画布空白处 → 整个设计器（含工具栏 + 页面树 + 画布 + 属性编辑器）弹到独立 Window，关闭弹窗自动恢复。便于多屏操作。

---

### 2.4 属性编辑器

右栏顶部 **4 Tab**：

| Tab | 内容 |
|---|---|
| **属性** | 当前选中控件的所有 PropertyDescriptor，按 Category 分组（常规/外观/格式/限值/安全/闪烁/字体/边框/布局/...） |
| **动画** | 6 类动画配置（外观/可见性/4 种移动） |
| **事件** | 6 个生命周期事件 × 多动作链 |
| **文本** | 当前控件用到的多语言文本 |

#### 字段类型化编辑器（属性 Tab）

不同字段类型自动渲染对应编辑器：

| 字段类型 | UI |
|---|---|
| String / Number / Integer | 普通 TextBox |
| Boolean | True/False ComboBox |
| Color | 色块预览 + hex 输入 + Popup（8 工业色 / 6 样式引用 / 自定义） |
| TagAddress | TextBox + 🔍 + **TagAutoComplete**（输入时弹下一级候选） |
| PageRoute | ComboBox（固定页 + 用户页） |
| Enum | ComboBox（选项带中英对照） |

字段右侧 🔶 小图标可点击切换"静态值 / 绑定变量"。

---

### 2.5 数据绑定

「属性」Tab 顶部「**数据绑定**」分组：
- **读取地址（订阅）**：控件订阅此变量，PLC 端值变化时控件自动刷新
- 留空 = 不订阅，控件只响应点击不显示状态

读取地址 ≠ 写入地址。写入地址在「事件」Tab 的动作步骤里独立配置（双地址模型）。

#### Quality Code 显示

订阅的 OPC UA 变量带质量信号：
- Good → 正常显示
- Uncertain → 值末尾加 `⚠`
- Bad → 显示 `####` + 灰底 + 红边框（数据不可信）

---

### 2.6 事件与动作

「事件」Tab 左侧 6 个事件触发点：

| 事件 | 触发时机 |
|---|---|
| 单击 | 鼠标按下→释放都在控件内 |
| 按下 | 鼠标按下瞬间 |
| 释放 | 鼠标松开瞬间 |
| 激活 | 控件获得焦点（Tab 流转到它） |
| 取消激活 | 失去焦点 |
| 数值更改 | 输入控件值变化时 |

#### 每个事件挂动作链

右侧动作列表卡片：
- **↑↓** 排序
- **×** 删除
- **＋ 新建动作**：弹「系统函数选择」分类菜单：
  - **编辑位**：置位 / 复位 / 取反位 / 按下时置位
  - **写入变量**：写入布尔 / 整数 / 浮点 / 加 1 / 减 1
  - **画面**：跳转页面（固定页/用户页）/ 返回 / 打开弹窗
  - **报警**：确认当前报警 / 清空报警缓冲
  - **其它**：显示对话框 / 播放声音

每个动作步骤按其类型自动生成参数编辑器：
- TagAddress 字段自动挂 TagAutoComplete
- PageRoute 字段走 NavigateTargets 下拉
- Bool / Number 字段对应类型化编辑器

#### 按钮特殊模式

按钮（button）控件支持 **Push / Toggle 双模式**：
- **Push**：点击发一次性事件
- **Toggle**：点击读 stateTag 后写反值，View 按 stateTag 当前值显示 onText / offText / onPicture / offPicture

#### 写 PLC 确认 + 审计

所有写入操作走「**WriteWithConfirm**」流程：
- 3 秒超时（可改）
- 失败 → 顶部系统消息显示"写入失败：{tagId} ({错误码})"
- 成功失败都写审计 trail（SQLite `data/audit.db`，90 天自动滚动）

---

### 2.7 动画系统

「动画」Tab 左侧 8 项导航：

| 类别 | 说明 |
|---|---|
| 总览 | 列出已配置的动画 + 跳转 / 删除 |
| **外观** | 变量 + 类型（范围 / 多位 / 单位）+ 行表（范围/背景/前景/闪烁） |
| **可见性** | 变量 + 条件（True / False / 范围内）+ 否则隐藏 / 禁用 |
| **水平移动** | 变量值映射到 X 像素范围 |
| **垂直移动** | 同上 Y |
| **直接移动** | XTag + YTag 双变量直接驱动 |
| **对角线移动** | 同时双变量映射 |

#### 外观动画（范围式）举例

变量 `DB1050.Cmd.TeachOn`，类型范围：

| 范围 | 背景色 | 前景色 | 闪烁 |
|---|---|---|---|
| 0 | 灰 | 黑 | 否 |
| 1 | 绿 | 黑 | 否 |
| 2 | 黄 | 黑 | 是 |
| ≥3 | 红 | 白 | 是 |

值落到 2 → 控件背景变黄并闪烁；值变 0 → 恢复灰底。

闪烁全局 300ms 时钟同步（多控件闪烁不会节奏不同步）。

#### 字段动态化 🔶

属性 Tab 里每个颜色/文本字段右侧的 🔶 小图标：
- 灰色 = 静态值
- 橙色 = 已绑变量
- 点击切换 → 弹卡片选「静态 / 绑变量 / 绑脚本」

---

### 2.8 多语言文本

「文本」Tab 列出当前控件用到的所有 `{text:xxx}` 引用。

#### 引用语法

控件 Text / Tooltip 字段填 `{text:welcome}` → 运行时按当前语言查 `Document.TextResources.Entries["welcome"][currentLang]`。

#### 编辑入口

顶部「**文本**」按钮 → 「**多语言文本资源**」对话框：
- 左侧 key 列表
- 右侧多语言列（zh-CN / en-US / ja-JP …）
- 增 / 删 / 改 key
- 顶部「**zh-CN**」下拉切换当前显示语言，所有控件 `{text:xxx}` 引用即时刷新

---

### 2.9 Faceplate（自定义复合控件）

WinCC 风格的复合控件封装。

#### 创建 Faceplate

1. 顶部「**Faceplate**」按钮切换到 Faceplate 编辑模式
2. 左栏从「页面列表」切换为「Faceplate 列表」，点 **+ 新建 Faceplate**
3. 填写名称 / 分类 / 默认尺寸 / 图标
4. 中间画布编辑 Faceplate 的**内部画面**（可拖普通 widget 进来）
5. 右栏「**接口属性**」分组添加接口参数：
   - Key（如 `deviceName`）
   - 类型（String / Number / Boolean / TagAddress / Color / PageRoute）
   - 默认值
6. 内部 widget 的字段用 `{prop:deviceName}` 引用接口属性

#### 实例化

退出 Faceplate 编辑模式 → 工具箱出现「**我的 Faceplate**」分组 → 拖入画布生成实例。

实例属性 Tab 显示「接口属性」分组，按 type 自动渲染对应编辑器，填实参值。

#### 版本管理

修改 Faceplate 内部 → Version 升 → 实例属性面板顶部出橙色 banner「Faceplate 已更新到 v1.x [升级]」，点升级按钮自动应用新版（新增 key 用默认值填，删除 key 保留）。

#### 内置 Faceplate

软件首次启动注入 4 个内置：气缸 / 轴 / 机械手 / 挡停，标「[内置]」前缀。基础骨架，用户可基于此修改增强。

---

### 2.10 全局样式 / 资源

#### 全局样式（顶部「样式」按钮）

- **色板**：9 个预设色（primary / accent / warn / error / text / textMuted / border / surface / surfaceAlt）
- **字体预设**：default / title / label / small

属性字段值可写 `{style:colors/primary}` 引用，改全局色板一处所有引用处联动。

#### 文本列表 / 图形列表（顶部「列表」按钮）

定义 INT → 文本/图片的映射表，符号 I/O / 图形 I/O 控件可引用：
- `{textList:status-list}` → 0 = 停止 / 1 = 运行 / 2 = 报警
- `{graphicList:state-images}` → 0 = `off.png` / 1 = `on.png`

#### 项目库 / 全局库

- **项目库**：当前工程内的可复用 widget 实例
- **全局库**：跨工程共享，存到 `%USERPROFILE%\.apexhmi\global-library.json`

存入：选中画布上 widget → 顶部「🔖 收藏」（项目库）/「🌍 全局库」按钮
使用：工具箱「我的库」/「全局库」分组拖入画布

#### 符号库

工具箱内置 15 个工业符号（阀 / 泵 / 电机 / 罐 / 管 / 流向箭头 / …），拖入画布即生成 graphic-view + IconKind。

---

### 2.11 项目导出 / 导入

#### 导出（顶部「📦 导出」）

弹保存对话框，输出 `.zip`，内含：
- ProjectDocument JSON
- assets/ 图片资源目录
- 配方 / 用户 / Faceplate / 样式 / 文本资源

#### 导入（顶部「📥 导入」）

弹选择对话框，选 `.zip` → 解压并加载工程，自动迁移旧 schema。

适用场景：客户现场快速复制工程；开发机 → 部署机一致性保证。

---

### 2.12 离线模拟

#### 启用

顶部「**〰 模拟**」开关切换。

#### 假数据生成

SimulationService 每 200ms 推送：
- `SimTag1` → sin 波（0-100）
- `SimTag2` → 随机数
- `SimTag3` → 阶梯递增
- `SimTag4` → 方波
- `SimTag5` → 三角波
- `SimBool1-3` → 每 3 秒翻转
- `SimText1` → 随机文本

把控件 variable 绑到这些 SimTag → 立即看到动态效果，无需连真实 PLC。**给客户演示必备**。

---

## 三、快捷键速查

| 快捷键 | 功能 |
|---|---|
| `Ctrl + S` | 保存工程（设计器自动保存 800ms 防抖）|
| `Ctrl + C` | 复制选中控件 |
| `Ctrl + V` | 粘贴 |
| `Ctrl + Z` | 撤销 |
| `Ctrl + Y` | 重做 |
| `Delete` | 删除选中控件 |
| `Tab` | I/O 域焦点跳到下一个 |
| `Shift + Tab` | I/O 域焦点跳到上一个 |
| `Enter` | I/O 域输入提交 |
| `Esc` | I/O 域输入撤销恢复原值 |
| `双击画布空白` | 弹出独立设计器窗口 |
| `Ctrl + D1`...`Ctrl + D9` | 主导航 1-9 号 Tab 切换 |

---

## 四、常见问题

### Q1：导入 PLC 变量很慢 / 卡死

A：第一次导入按 OP 工位号筛选——只勾选你工程实际用的 OP，避免一次性加载全部数十万变量。
- 顶部「PLC 变量」按钮 → 弹工位选择对话框 → 默认全选改为只选用到的几个 OP
- 设置保存到 `config/plc-variable-import.json`，下次启动自动按上次选择加载

### Q2：写 PLC 不生效

A：检查：
1. **质量信号**：控件是否变 `####`？变了说明 PLC 连接异常
2. **顶部系统消息**：是否提示"写入失败：tagId (错误码)"？
3. **审计日志**：打开 `data/audit.db`，查 success=0 的最近记录拿错误详情
4. **权限**：控件属性是否设了 `authorization`？当前用户角色不匹配会拒

### Q3：旧工程加载后控件显示 `[typeId]` 占位

A：那个 widget 类型在 P0 清洗时被删除（旧业务复合控件）。
- 如果是 `manual-cylinder-block` 等：用 Faceplate 体系的「我的 Faceplate → [内置] 气缸」拖一个新实例取代
- 如果是 `motor` / `alarm-banner` 等：用 I/O 域 + Appearance 动画自行组合实现

### Q4：模拟模式下趋势曲线不动

A：检查趋势 trace 配置的 variable 字段是否填了 `SimTag1` 这类模拟变量名。模拟模式只推 SimTag* / SimBool* / SimText* 这些约定 tag。

### Q5：自动程序生成后 Git 没 push

A：检查 Git 拉取面板「**生成程序后自动 commit + push 到远端**」是否勾选。未勾选时只在本地工作区追加文件，不会自动提交。

### Q6：Faceplate 实例显示「未知 Faceplate」

A：工程文件可能引用了被删除的 Faceplate ID。打开 Faceplate 编辑模式，检查左栏 Faceplate 列表是否含该 ID。如果是内置但被误删，重启软件会自动重新注入。

---

## 附录：技术架构（简）

- **WPF + .NET Framework 4.8**
- **MVVM**：CommunityToolkit.Mvvm
- **OPC UA**：OPCFoundation.NetStandard.Opc.Ua
- **图表**：OxyPlot
- **嵌入浏览器**：Microsoft.Web.WebView2
- **历史归档 / 审计**：SQLite（System.Data.SQLite）
- **工程文件**：System.Text.Json
- **设计器**：Schema-driven 属性面板（动态 PropertyDescriptor 渲染）
- **运行时**：DynamicPageHost + WidgetRegistry 工厂模式

---

文档维护：随软件升级同步更新。如发现描述与实际不符，提 issue 或直接编辑本文档 PR。
