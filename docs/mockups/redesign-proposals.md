# 固定页 UI + 功能完善建议清单（v0.2）

> **用法**：通览全表，**只在"你的预期"列写差异**——同意我的设计就留空、不要这条写"不要"、要改写"改成XX"。全部填完一次性发回，我再统一更新 HTML 范例 + 动 WPF 代码。
>
> **范围声明**：
> - 已实现的核心功能不动（长按启动 / Sparkline / 双 IO 表 + 翻页 / 程序 trace 图缩放双光标 / 参数权限染色 / 设计器布局开关 等）
> - 仅新增 / 整合 / 优化体验（功能层面 + 视觉层面）
> - 涉及"生产计数模块改 PLC 简化合约 + SQLite 账本"的部分按 [count-module-design.md](count-module-design.md) 走，不在本清单展开
> - 视觉范例见 [redesign-preview.html](redesign-preview.html)
>
> **优先级标记**：⭐ 强推荐（明显提升日常使用） · ◯ 一般 · △ 锦上添花
>
> **类别标记**：[F] 功能新增 · [V] 视觉/交互优化 · [B] 修复已知 bug

---

## 执行进度（截至 2026-05-08 19:30）

### ✅ 已完成 commit

| Phase | 范围 | commit | 项数 |
|---|---|---|---|
| 0.1 | M1+M2 监控 9 处 bug + 4 命名属性 | `89283a6` | 2 |
| 0.2 | L2 + L3 用户外置（IUserService + config/users.json + SHA256） | `89283a6` | 2 |
| 0.3 | G3 状态栏流程步号 + 节拍/UPH chip | `89283a6` | 1 |
| 0.4 | G1 全局快捷键 Ctrl+1~8 / NumPad1-8 + F5 | `89283a6` | 1 |
| 0.5 | G6 多语言切换（编辑菜单 → 语言 / Language） | `89283a6` + `83bf45f` | 1 |
| 0.6 | L7 最近登录时间 + 失败次数显示 | `89283a6` | 1 |
| 0.7 | G7 全局未保存提示（DesignerEditor 接入） | `89283a6` | 1 |
| 1 | 生产计数模块 HMI 侧（SQLite + 差值法 + CountView Tab 11 + B 入口） | `1b8c964` + `e3dcf7f` + `e35b5a3` | 1 |
| **2.1** | **主界面 H1/H2/H3/H4/H5/H6/H7** | `ff00bb5` + `42ef0d7` | **7** |
| **2.2.AB** | **监控-详细生产 M3/M5/M6/M8 + IO 监控 M9/M10/M11/M13/M15** | `8d2c945` | **9** |
| **2.2.CD** | **监控-通讯调试 M17/M19/M20 + 程序监控 M21/M22** | `fac61a7` + `8ddcabc` | **5** |
| **2.3** | **手动操作 MA2/MA5/MA6/MA7/MA9/MA10** | `b6f67a0` | **6** |
| **2.4** | **参数 P1/P2/P3/P5/P6/P7/P8/P9/P10**（P4 不做） | (本次) | **9** |

**累计完成：约 46 项 / 13 commit**

### ⏸ 推迟到 Phase 3 / 4（设计复杂或视觉重构相关）

| 项 | 推迟到 | 原因 |
|---|---|---|
| G2 高 DPI UI 缩放 | Phase 3 视觉 | XAML ScaleTransform 方案需深设计 |
| G10 权限边界 disabled+tooltip | 单独 phase | 50+ 按钮工程量大 |
| H10 KPI 图标徽章状态色 | Phase 3 视觉 | 跟全局视觉一起刷 |
| H12 首页可定制 | Phase 4 锦上添花 | 拖拽+持久化 |
| M14 DI/DO 切换改 tab 视觉 | Phase 3 视觉 | 视觉调整 |
| M16 OPC UA 节点收藏夹 | Phase 4 | persistence + 数据结构 |
| M18 OPC UA 写入测试 | Phase 4 | dialog + 权限审计 |
| M23 流程异常排行跳转 trace 图 | Phase 4 | 实现复杂 |
| M24 标记关键步号 | Phase 4 | 持久化 |
| M25 历史回放速率 | Phase 4 | 要新加 ReplayTimer |
| M26 看报警弹层 | Phase 4 | 跟报警增强一起做 |
| M27 PDF 报告 | Phase 4 | 要 PDF 库 |
| MA4 气缸分组 Tab | Phase 4 | 要 ManualCylinderBlockItem 加 Group 字段 |
| MA8 设计器布局应急按钮 | 不做 | 用户没填 |
| P4 修改前后对比 diff 弹层 | 不做 | 用户反馈不需要（行级 IsDirty 黄底已够直观） |

### ⏳ 待做（按 Q3=全做 推进）

| Phase | 子页 | 项数 | 关键内容 |
|---|---|---|---|
| **2.5** | **报警 AlarmView** | 10 项 (A1-A10) | 声音+弹窗 / 级别筛选 tab / 详情侧边栏 / 导出 CSV-Excel / 频率图 / 二次确认 / 备注 / 跳流程 / 时间段查询 / 邮件 IM 推送 |
| **2.6** | **配方 RecipeView** | 9 项 (R1-R9) | 对比 / 导入导出 / 启用前预校验 / 切换流程化 / 历史版本 / 删除二次确认 / 上次使用时间 / 产线兼容标签 / 试运行 |
| **2.7** | **程序生成 DesignerView** | 6 项 (D1/D2/D3/D6/D8/D9) | IO 表导入校验 / 生成后 diff 预览 / 多工位批量生成 / Git 代理 / 快速编辑注释 / ST 文件预览 |
| **2.8** | **履历 AuditView** | 8 项 (AU1-AU8) | 时间段过滤 / 用户动作过滤 / 关键字搜索 / 导出 PDF / 事件链 / 动作分类 / 详情截断 / 履历归档 |
| **2.9** | **登录 LoginView 升级** | 3 项 (L4/L5/L6) | 切换不退出 / 临时提权 / 权限说明可编辑 |
| Phase 1 联调 | 等 PLC | — | PLC 端 DB8003_Count 简化合约 + Tags 加 OK.Total/NG.Total |
| **Phase 3** | **整体视觉重构** | 全局 | XAML Style 资源字典 + 卡片/按钮/表格全局风格刷新 + KPI/环形进度等新视觉组件 |
| **Phase 4** | **锦上添花** | 14+ 项 | G4 事件中心 / 上面"推迟"列表里的项 |

### git 历史

```
(本次) [OP-Phase-2.4] 参数页 P1-P10
34b9847 [OP-doc] 更新 redesign-proposals.md 执行进度到 Phase 2.3
b6f67a0 [OP-Phase-2.3] 手动操作 MA2/MA5/MA6/MA7/MA9/MA10
8ddcabc [OP-Phase-2.2.CD-fix]
fac61a7 [OP-Phase-2.2.CD] 监控-通讯+程序监控 M17/M19/M20/M21/M22
8d2c945 [OP-Phase-2.2.AB] 监控-详细生产+IO M3/M5/M6/M8/M9/M10/M11/M13/M15
42ef0d7 [OP-Phase-2.1-fix] ProgressBar Mode=OneWay
ff00bb5 [OP-Phase-2.1] 主界面 H1-H7
83bf45f [OP-Phase-0.5-tweak] 语言切换挪到编辑菜单
e35b5a3 [OP-Phase-1-fix2] x64 e_sqlite3.dll
e3dcf7f [OP-Phase-1-fix] SQLitePCL Init
1b8c964 [OP-Phase-1] 生产计数模块 HMI 侧
34a4882 Merge OP-pre + OP-Phase-0
89283a6 [OP-Phase-0] 修 MonitorView 9 处 bug + 用户外置 + 全局基础
fd17a2d [OP-pre] 画布设计器顶部横向工具箱重构 + PLC 变量导入
807ff35 master ← 干净未污染
```

---

## 全局共性（所有页面共享）

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| G1 | ⭐ | F | **全局快捷键**：Ctrl+1-8 切顶部图标导航 / F1 帮助 / F5 刷新 / Esc 取消当前操作 | 做|
| G2 | ⭐ | F | **顶部图标导航支持高 DPI 字体放大**（当前 90×90 固定，竖屏/4K 屏布局错乱） |做 |
| G3 | ◯ | V | 状态栏增加"当前主流程步号 + 节拍"实时小条（不切页就能看），目前底部只有模式/角色/PLC | 做|
| G4 | ◯ | F | 全局右上角加一个"事件中心"小铃铛（聚合最近报警 / 操作 / 系统消息），点开侧抽屉浏览 |做 |
| G5 | △ | V | 主题切换：浅色（当前）/ 深色 / 高对比度（车间环境光强或值班灯下用）—— 仅切换调色板，不动布局 |做 |
| G6 | ⭐ | F | **多语言切换**：当前已有 i18n 资源，加一个状态栏右下角的语言切换菜单（中/英/日，按需） | 做|
| G7 | ⭐ | F | **未保存提示**：参数 / 配方 / 设计器有未保存改动时切页弹确认，避免误丢失 | 做|
| G8 | ◯ | F | 底部状态栏 PLC 状态点击 → 跳到监控-通讯调试页 |不做 |
| G9 | △ | V | 左侧子导航支持折叠（点击群标题收起该组），减少滚动 |不做 |
| G10 | ⭐ | F | **操作员/工程师/管理员权限边界更明确**：当前部分按钮在低权限下"可点击但弹不允许"，建议统一改成 disabled + tooltip 说明所需角色 |做 |

---

## 页面 1：主界面（HomeView）

**核心功能（保留）**：6 KPI / 模式切换 / 长按启动 / 紧急停止 / 报警复位 / 3 ready 卡 / 自动流程当前步号 / 流程日志 / 重点报警

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| H1 | ⭐ | F | **目标完成度环形进度**：当前班次产量 ÷ 目标 = 56.9%（环形 +中心数字），比纯数字直观 |做 |
| H2 | ⭐ | F | **班次倒计时**：白班还剩 3:42 → 班次切换时主动弹提示（让操作员准备交班） |做 |
| H3 | ⭐ | F | **OEE / 产量 / 节拍 KPI 卡里嵌 mini sparkline**（最近 30 min 趋势缩略图） |做 |
| H4 | ⭐ | F | **重点报警卡可点击 → 直接跳到报警页对应记录**（当前是纯展示） |做 |
| H5 | ⭐ | F | **启动条件 unmet 时显示是哪几条没过**（点击 ready 卡展开明细，比如"模式 Ready"展开显示 → 模式 = 自动 ✓ / IO 联锁 ✓ / 安全门 ✗） | 做|
| H6 | ◯ | F | **流程日志过滤器**：仅看错误 / 警告 / 信息 三色筛选，避免一直滚 |做 |
| H7 | ◯ | F | **流程日志支持搜索**（关键字过滤，比如查某个步号的所有事件） | 做|
| H8 | ◯ | F | **模式切换二次确认**：从"自动"切回"手动"在生产中弹确认（避免误碰） |不做 |
| H9 | ◯ | F | **快捷键提示**：长按启动支持 SPACE 键、紧急停止 Ctrl+S、报警复位 Ctrl+R |不做 |
| H10 | ◯ | V | **6 KPI 卡的图标徽章**改成与状态联动的色（运行=绿/停机=灰/告警=橙/异常=红），不只标签色 | 做|
| H11 | △ | F | **生产线条状图**（横向显示工位 1→8，每个工位标当前节拍 + 是否在工作位） |不做 |
| H12 | △ | F | **首页可定制**：右上角加"编辑首页"按钮，让用户拖拽调整 KPI 顺序 / 隐藏不关心的卡片（个人首选项存本地） | 做|
| H13 | △ | B | RuntimeHeaderText 当前在某些状态下显示空字符串，建议加默认文案 |做 |

---

## 页面 2：监控（MonitorView）

监控分 4 子页，逐一列：

### 2A. 详细生产数据子页

**核心功能（保留）**：筛选条 / 4 KPI / 历史趋势落盘说明 / 4 趋势小图

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| M1 | ⭐ | B | **修 4 处绑定字面量 bug**（已在 [monitor-view-audit.md:37-43](monitor-view-audit.md) 提过：`"班次：{Binding...}"` 当字面量显示） | 做|
| M2 | ⭐ | B | **修 5 处硬编码 Tags[25/26/29/30] 下标**，改成命名 Tag 解析 | 做|
| M3 | ⭐ | F | **KPI 卡可点击展开历史明细**（点"班次产量" → 弹层显示本班分时产量 24 行） |做 |
| M4 | ⭐ | F | **趋势图加 X/Y 缩放滚轮 + 双击复位**（当前只能看不能缩） |做 |
| M5 | ⭐ | F | **时间范围加"自定义"日期选择**（当前只有近 1h/8h/24h 三档） |做 |
| M6 | ◯ | F | 工单 / 配方切换时的对比高亮（前一工单 vs 当前工单的 KPI 差异显著时高亮） | 做|
| M7 | ◯ | F | "导入流程 CSV 分析"完成后给出结果摘要弹层（当前似乎是静默处理） | 不做|
| M8 | ◯ | F | 趋势卡支持切换显示项（产量 / OEE / 报警 / 节拍 / 流程步号 / 异常）的多选勾选 | 做|

### 2B. IO 监控子页

**核心功能（保留）**：DI/DO 切换 / 左右双表 / 翻页

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| M9 | ⭐ | F | **加搜索框**（按地址 / 注释快速过滤，比如输入 "CY3" 只看 CY3 相关 IO） |做 |
| M10 | ⭐ | F | **加翻转计数列**（每个点统计今日 ON↔OFF 翻转次数，识别抖动） | 做|
| M11 | ⭐ | F | **信号变化高亮**（最近 1 秒内变化的点底色闪烁 1 秒，便于跟踪触发） | 做|
| M12 | ◯ | F | **双击 DO 点位 → 强制写入弹层**（仅工程师 + 设备非自动模式时可用，带二次确认 + 审计） | 不做|
| M13 | ◯ | F | 翻页按钮太大（50×50），加键盘 PageUp/Down 支持 |做 |
| M14 | ◯ | F | **DI/DO 切换 toggle 改成顶部固定 tab**，翻页时不被遮挡 | 做|
| M15 | △ | F | 加"导出当前页 IO 状态快照 PNG/CSV" | 做|

### 2C. 通讯调试子页（OPC UA）

**核心功能（保留）**：浏览根节点 / 读取选中 / 加入变量表 / TreeView / 节点详情

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| M16 | ⭐ | F | **节点收藏夹**：常用 Tag 加星，下次直接展开（当前每次都要从根浏览） |做 |
| M17 | ⭐ | F | **节点搜索框**：输入名称模糊匹配（NodeId / DisplayName） |做 |
| M18 | ⭐ | F | **写入测试按钮**（数值类节点）：弹小窗写入测试值 + 立即回读验证（带审计） |做 |
| M19 | ◯ | F | 节点订阅期间的 quality 变化日志（短期保留，用于诊断断连） | 做|
| M20 | ◯ | V | 节点详情面板配色不一致（部分白底 + 浅蓝字、部分深底），统一成浅色 | 做|

### 2D. 程序监控子页（Trace）

**核心功能（保留）**：流程步号图 / 双光标 / 缩放 / 暂停采样 / 历史回放 / 导出 CSV / 保存加载会话 / 流程步表 / 异常排行榜

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| M21 | ⭐ | F | **双光标 A↔B 之间的耗时统计 + 平均节拍**（当前只显示 Δt，加平均/最大/最小） | 做|
| M22 | ⭐ | F | **导出 PNG 截图**（当前只能导 CSV，截图便于汇报） |做 |
| M23 | ⭐ | F | **流程异常排行榜可点击 → 直接跳到该步号位置**（图上居中显示） |做 |
| M24 | ◯ | F | 加"标记关键步号"功能，标记后图上 + 表上高亮，便于交班讲解 |做 |
| M25 | ◯ | F | 历史回放支持播放速率（1× / 2× / 5×），当前只能拖滑块 | 做|
| M26 | ◯ | F | "看报警"按钮当前要返回报警页，做成弹层显示报警详情（不离开监控页） | 做|
| M27 | △ | F | 流程异常报告导出加 PDF（含图截图 + 排行榜 + 处理建议），方便发邮件汇报 |做 |

---

## 页面 3：手动操作（ManualView）

**核心功能（保留）**：🎨 设计器布局开关 / 气缸卡片网格 / 轴 / 机械手 / 挡停 / 流程

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| MA1 | ⭐ | F | **气缸卡片可视化方向箭头**：→ 工作位 / ← 原点位 用动画箭头 + 当前位置指示（不只文字） |不做 |
| MA2 | ⭐ | F | **气缸卡片报警态视觉**：超时报警时整卡片红边框 + 闪烁（当前只是状态文字变红） | 做|
| MA3 | ⭐ | F | **一键"全部回原点"按钮**（带二次确认 + 仅手动模式下可用），日常下班/换型用得上 | 不做|
| MA4 | ⭐ | F | **气缸分组 Tab**（按工位 OP30/OP40/OP50 或按类别"上料/夹紧/出料"分组），减少滚动 |做 |
| MA5 | ⭐ | F | **气缸搜索框**（按名称或 IO 地址），调试时定位快 |做 |
| MA6 | ◯ | F | 双击气缸名 → 跳到参数页对应行（比如双击 CY3 → 高亮 CY3.WorkTimeout） | 做|
| MA7 | ◯ | F | 长按气缸卡片 → 显示完整 IO 详情弹层（DI/DO/超时设定/最近 10 次动作记录） |做 |
| MA8 | ◯ | F | **设计器布局开关右侧加"恢复硬编码"应急按钮**：设计器布局误删时一键回老 UI | |
| MA9 | ◯ | F | 轴 / 机械手页同样加分组 + 搜索 |做 |
| MA10 | △ | F | 气缸操作日志（点击 → 输出动作）小条显示在卡片底部，最近 1 条 | 做|

---

## 页面 4：参数设定(ParameterView)

**核心功能（保留）**：连接 / 断开 / 加载 / 保存 / 大 DataGrid（分组 / HMI 变量 / PLC 变量 / 绑定方向 / 说明 / 最小权限 / 编辑状态）

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| P1 | ⭐ | F | **顶部加搜索框**（按变量名 / PLC 地址 / 说明文本过滤），表大时一行行翻太累 |做 |
| P2 | ⭐ | F | **分组折叠**（点击分组名收起该分组所有行） |做 |
| P3 | ⭐ | F | **未保存改动行高亮黄色**（避免误切走丢失修改），全局共性 G7 也覆盖这条 |做 |
| P4 | ⭐ | F | **修改前后对比**：保存按钮旁加"查看本次修改"，弹层显示 diff 列表（哪个参数 X→Y） |做 |
| P5 | ⭐ | F | **变更历史列**（最近 10 次"谁 / 何时 / 改了什么 / 旧值 / 新值"），点击行右侧小图标弹出 |做 |
| P6 | ◯ | F | **与配方对比按钮**：选某配方 → 高亮表中所有差异行 + 一键"应用配方到全表" |做 |
| P7 | ◯ | F | **权限不足时的优雅提示**：当前操作员点编辑会怎样？建议改成 disabled + tooltip "需要工程师权限" + "申请提权"链接 | 做|
| P8 | ◯ | F | **参数合法性校验**：输入超过最小/最大限值时框红 + 阻止保存（需要在 schema 里定义范围） | 做|
| P9 | ◯ | F | 加"导出当前参数为配方"快捷按钮，跳转配方页传入 |做 |
| P10 | △ | F | 加批量编辑（多选行 + 同时改某列） |做 |

---

## 页面 5：报警画面（AlarmView）

**核心功能（保留）**：4 子页（当前 / 历史 / 日志 / 统计）/ 确认全部 / 复位全部 / 保存历史 / 加载历史 / 重点处理建议

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| A1 | ⭐ | F | **报警声音 + 弹窗**：高级别 + 未确认时主动弹层并响铃（声音文件可在系统参数页配） |做 |
| A2 | ⭐ | F | **按级别 / 来源筛选 tab**（高/中/低 + 来源下拉），当前只能整表看 |做 |
| A3 | ⭐ | F | **报警详情侧边栏**：点击行 → 右侧抽屉显示完整描述 / 处理建议 / 关联流程步号 / 历史发生记录 |做 |
| A4 | ⭐ | F | **一键"导出当前筛选结果为 CSV/Excel"**，质量部要数据时方便 | 做|
| A5 | ⭐ | F | **报警发生频率图**（24h / 7d 直方图，按级别堆叠），趋势比纯数字直观 | 做|
| A6 | ◯ | F | "确认全部"按钮加二次确认（错点会清整页未读） |做 |
| A7 | ◯ | F | 报警条目支持加备注（操作员处理后写一句"已检查电磁阀线圈"） |做 |
| A8 | ◯ | F | **关联流程跳转**：当前已有"看流程"按钮（统计页），扩展到当前页和历史页 |做 |
| A9 | ◯ | F | 历史报警支持时间段查询 + 关键字搜索 | 做|
| A10 | △ | F | 报警发送邮件 / IM 推送（高级别自动通知值班工程师），需要外部接口配置 | 做|

---

## 页面 6：配方管理（RecipeView）

**核心功能（保留）**：配方下拉 / 加载到参数 / 新建 / 复制 / 删除 / 用当前参数覆盖配方 / 加载文件 / 保存文件 / 配方列表 / 当前配方参数快照

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| R1 | ⭐ | F | **配方对比**：选两个配方 → 弹层显示差异表（仅显示不同的参数行） |做 |
| R2 | ⭐ | F | **配方导入 / 导出 CSV / JSON**（当前是整文件 load/save，要求精细化） | 做|
| R3 | ⭐ | F | **配方启用前预校验**：参数合法性 / 与当前生产兼容性（比如 NG 仓非空时禁止切配方） | 做|
| R4 | ⭐ | F | **切换配方流程化**：停机 → 切换 → 启动（向导式步骤，避免漏步），不仅是"加载到参数" | 做|
| R5 | ⭐ | F | **配方历史版本**：每次"用当前参数覆盖配方"自动留快照，可回滚到任一历史版本 | 做|
| R6 | ◯ | F | "删除当前"加二次确认 + 删除原因输入（审计） |做 |
| R7 | ◯ | F | 配方列表加"上次使用时间"列（按使用频次排序） |做 |
| R8 | ◯ | F | 配方加产线兼容标签（PRD-A 仅 OP30 可用 / PRD-B 全工位可用） | 做|
| R9 | △ | F | 配方加"试运行模式"：加载到参数后空跑 N 件验证再正式启用 |做 |

---

## 页面 7：程序生成（DesignerView）

**核心功能（保留）**：手动 / 自动 / 初始化 三入口 / IO 表导入 / PLC 模板 / 工位号 / Git 仓库配置 / 5 个勾选选项 / 拉取代码 / 生成程序 / 自动 commit + push / 执行日志

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| D1 | ⭐ | F | **IO 表导入校验**：地址重复  / 类型不匹配 → 高亮报错行 + 阻止生成 |做 |
| D2 | ⭐ | F | **生成程序后差异预览**：diff 视图（左：Git 当前 .st，右：生成后），方便审核才 commit |做 |
| D3 | ⭐ | F | **多工位批量生成**：勾选多个工位 OP30/OP40/OP50 一次跑 |做 |
| D4 | ◯ | F | **生成日志可保存为模板**：固定一套设置后导出 .json，下次"加载模板"复用 |不做 |
| D5 | ◯ | F | "强制覆盖本地 reset --hard" 按钮加红字警告 + 二次确认（当前只是红字，没拦截） |不做 |
| D6 | ◯ | F | Git 拉取支持代理配置（场内代理需求） |做 |
| D7 | ◯ | F | "生成程序"按钮长按 / 二次确认（避免误触跑全表） |不做 |
| D8 | ◯ | F | 当前 IO 表预览只读，加"快速编辑注释"功能（不必导出再改 Excel 再导入） |做 |
| D9 | △ | F | 生成的程序 ST 文件加预览（点击列表中的 .st → 显示内容） |做 |

---

## 页面 8：操作履历（AuditView）

**核心功能（保留）**：操作履历表（时间 / 用户 / 动作 / 目标 / 结果 / 详情）

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| AU1 | ⭐ | F | **时间段过滤**（最近 1h / 8h / 24h / 7d / 自定义） | 做|
| AU2 | ⭐ | F | **用户 / 动作类型过滤**（多选 dropdown） | 做|
| AU3 | ⭐ | F | **关键字搜索**（搜目标 / 详情列） | 做|
| AU4 | ⭐ | F | **导出 PDF 报表**（按时间段，含汇总）—— 质量审核会要 |做 |
| AU5 | ◯ | F | **事件链关联视图**：同一报警从触发→确认→复位→恢复，串成一条链一起展示 |做 |
| AU6 | ◯ | F | 表格列加"动作分类"标签（登录类 / 设备操作 / 参数修改 / 报警处理 / 系统事件） |做 |
| AU7 | ◯ | F | 详情列过长截断 + 鼠标悬停显示完整内容 |做 |
| AU8 | △ | F | 履历归档：超过 6 个月的履历自动 zip 归档到 `audit-archive/`，内存只保最近 6 个月 | 做|

---

## 页面 9：登录（LoginView）

**核心功能（保留）**：当前身份 / 密码 / 三角色按钮（操作员 / 工程师 / 管理员）/ 退出登录 / 权限说明

| # | 优先级 | 类型 | 建议项 | 你的预期 |
|---|---|---|---|---|
| L1 | ⭐ | F | **账号锁定**：连续输错 3 次锁 5 分钟（防试探），锁定状态显示在登录卡片 |不做 |
| L2 | ⭐ | F | **密码不在 UI 上展示**（当前底部"示例密码：工程师 123456 · 管理员 admin888"是硬编码暴露），建议挪到 docs 内部文档 |做 |
| L3 | ⭐ | F | **用户数据外置**：当前是硬编码 3 角色 + 1 套密码，建议挪到 `config/users.json`（哈希存储），支持多用户 |做 |
| L4 | ⭐ | F | **切换用户但不退出会话**：避免每次切角色都退到登录页（仅显示登录弹层） | 做|
| L5 | ◯ | F | **临时提权**：当前角色不够时弹"工程师扫码 / 输密码授权 1 次操作"，类似 Windows UAC | 做|
| L6 | ◯ | F | **权限说明改为可编辑表**（管理员可在系统参数页改"工程师可不可以XX"） |做 |
| L7 | ◯ | F | 加最近登录时间 / 登录失败次数显示 |做 |
| L8 | △ | F | 支持 Windows 域账号 / LDAP 单点登录（场内有 AD 时启用） |不做 |
| L9 | △ | F | 操作员 / 工程师 / 管理员各自的"个人首页"（开机后默认跳哪页 + 哪些卡片可见） | 不做|

---

## 关键决定点（请单独回答，影响范围较大）

| # | 问题 | 你的回答 |
|---|---|---|
| Q1 | 优先级 ⭐ 项里有没有你**坚决不要**的？（避免我做了你又删） | 没有|
| Q2 | "完善功能"想覆盖哪几个层级：仅 UX、含新增功能、含核心模块改造（比如生产计数模块改 PLC 简化合约 + SQLite 账本）？ |全部 |
| Q3 | 工作量上：**全做** vs **先 ⭐ 项 vs 仅"核心修复 + 急用功能"**？我建议先做⭐ 项分批落地 |全做 |
| Q4 | 你希望我先把**全 9 页 HTML 完善版**做出来（看视觉），还是按你确认的清单**直接动 WPF 代码**（视觉以已有 HTML 为参考）？ |直接做 |
| Q5 | 多语言（G6）现在要不要做？还是只做中文 |做 |
| Q6 | 主题深色 / 高对比度（G5）—— 车间值班灯下需要吗？ |做 |
| Q7 | 个人首选项（H12 / L9）—— 要按用户存还是全局存？涉及登录系统升级（L3） |全局 |

---

## 文件外部依赖

待你确认后涉及的新增 / 修改：

- `Models/`：AlarmRecord 加 Note / Acknowledger / RelatedFlowStep / Severity 等字段
- `Services/`：用户管理 IUserService（替代硬编码 3 角色）/ 报警发送 INotificationService / 配方对比 IRecipeDiffService / 履历导出 IAuditExportService
- `config/`：`users.json` / `shift.json` / `theme.json` / `notifications.json`
- `Views/Pages/`：每页都会动，但布局骨架不变
- 新增 `Views/Dialogs/`：UnsavedChangesDialog / RecipeCompareDialog / IoForceWriteDialog 等

---

**填完后** 把这份 md 发回给我，我会：
1. 按你的预期更新 HTML 范例（每页都做完善版视觉）
2. 输出按页拆分的 WPF 实施 todo（每个 ⭐ 项一个 commit 粒度）
3. 等你点头后开始动代码（按 phase 分批，避免一个大 PR）
