# ApexHMI 开发任务清单（可自动执行版）

> 本清单按"原子任务"拆分，每个任务包含：**目标 / 输入 / 输出 / 验收标准**，以便自动化代理（Claude Code 等）按序执行。
> 任务编号：`阶段-子项-序号`，例如 `P0-LOG-01`。
> 完成后在文件头打勾或更新状态：⬜ 未开始 / 🟡 进行中 / ✅ 完成 / ❌ 阻塞

---

## 阶段 P0 · 止血（预计 5-7 工作日）

### P0-LOG · 引入结构化日志（Serilog）

**P0-LOG-01** ✅ 添加 Serilog 依赖
- 输入：`ApexHMI.csproj`
- 输出：新增 NuGet 包：`Serilog`、`Serilog.Sinks.File`、`Serilog.Sinks.Console`、`Serilog.Extensions.Logging`、`Microsoft.Extensions.Logging`
- 验收：`dotnet restore` / `dotnet build` 通过

**P0-LOG-02** ✅ 创建 `Services/Logging/LoggingBootstrapper.cs`
- 配置：滚动文件 `logs/apexhmi-.log`（按天，保留 14 天，单文件 ≤ 20MB），同时输出 Console
- 日志级别：Information（Release）/ Debug（Debug 配置）
- 模板：`{Timestamp:HH:mm:ss.fff} [{Level:u3}] [{SourceContext}] {Message:lj} {Exception}{NewLine}`
- 验收：调用 `LoggingBootstrapper.Configure()` 后 `logs/` 目录生成日志文件

**P0-LOG-03** ✅ 在 `Bootstrapper.cs` 注册 `ILogger<T>`
- 修改：`ServiceCollection.AddLogging(b => b.AddSerilog(Log.Logger, dispose: false))`
- 验收：任意 Service 构造函数注入 `ILogger<T>` 成功

**P0-LOG-04** ✅ 在所有 Service catch 块加日志 / 注释
- 范围：`Services/*.cs` 所有现有 `try-catch`
- 模板：`Log.Error(ex, "操作 {Op} 失败", nameof(MethodName));`
- 验收：grep `catch ` 后所有空 catch / 仅注释 catch 数量为 0
- 当前状态：已完成 `Services/` 下历史 `catch` 审计，恢复型异常补充 Serilog Debug/Warning/Error，诊断/崩溃报告兜底 catch 保留明确注释。

---

### P0-EXC · 全局异常拦截

**P0-EXC-01** ✅ 在 `App.xaml.cs` 注册三大异常入口
- `DispatcherUnhandledException` → 记录 + 弹窗 + `e.Handled = true`
- `AppDomain.CurrentDomain.UnhandledException` → 记录（IsTerminating 时不阻止）
- `TaskScheduler.UnobservedTaskException` → 记录 + `e.SetObserved()`
- 验收：手动 throw 测试三种异常路径都进入日志

**P0-EXC-02** ✅ 创建 `Services/Diagnostics/CrashReporter.cs`
- 功能：将异常 + 系统信息（OS、.NET 版本、内存、当前操作）写入 `logs/crash/crash-{timestamp}.log`
- 验收：异常发生后 `logs/crash/` 出现独立 dump 文件

---

### P0-ASY · 消灭 async void

**P0-ASY-01** ✅ 创建统一事件处理模板 `Views/Common/AsyncEventHandler.cs`
- 提供静态方法 `RunSafe(Func<Task> action, string operationName, bool showUserMessage = true)`
- 验收：方法签名通过单元测试

**P0-ASY-02** ✅ 改造 `Views/MainWindow.xaml.cs:343,354,372`
- 原 `async void Btn_Click` → 调用 `AsyncEventHandler.RunSafe`
- 验收：手动测试按钮点击；强制抛异常被日志捕获，UI 不崩溃

**P0-ASY-03** ✅ 改造 `Views/Pages/ParameterView.xaml.cs:31,39`

**P0-ASY-04** ✅ 改造 `Views/Pages/MonitorView.xaml.cs:262`

**P0-ASY-05** ✅ 改造 `Views/Dialogs/AxisSettingsWindow.xaml.cs:25` + `Views/Dialogs/CylinderSettingsWindow.xaml.cs`

**P0-ASY-06** ✅ 改造 `ViewModels/MainViewModel.Refresh.cs:352,437`
- 非事件入口的 `async void` → `async Task` + 调用方加 await，或加顶层 try-catch
- 验收：编译通过；功能回归

**P0-ASY-07** ✅ 全项目 grep `async void`，确认只剩事件处理器
- 验收：`grep -r "async void" --include="*.cs"` 输出全部为事件订阅签名

---

### P0-SEC · 凭据加密 + 空 catch 修复

**P0-SEC-01** ✅ 创建 `Services/Security/SecretProtector.cs`
- 实现：基于 `System.Security.Cryptography.ProtectedData`（DPAPI、CurrentUser scope）
- API：`string? Protect(string?)` / `string? Unprotect(string?)`，密文带 `ENC:` 前缀
- 验收：单元测试 round-trip 通过

**P0-SEC-02** ✅ 改造 `ConfigurationService.SaveAsync`
- Save 前 snapshot 明文 + 加密；Save 完 finally 中恢复明文（避免污染内存）
- 验收：旧明文配置可平滑迁移；运行中 OPC / Git 仍能正常使用

**P0-SEC-03** ✅ 改造 `ConfigurationService.LoadAsync`
- 在所有 partition 加载完成后调用 `DecryptSecrets`（位置错了会导致 GitPull token 仍是密文）
- 验收：重启后 OPC 密码 / Git Token 都能解密回明文

**P0-SEC-04** ✅ 修复 `Converters/StringToBrushConverter.cs:22` 空 catch
- 改为：`catch (Exception ex) { Log.Warning(ex, "..."); return Brushes.LightGray; }`
- 验收：异常被记录

---

### P0-CSPROJ · csproj 配置

**P0-CSPROJ-01** ✅ 添加 `<Reference Include="System.Security" />` 到 csproj
- 原因：DPAPI 在 net48 需要 `System.Security.dll`
- 验收：构建无 `ProtectedData` 找不到错误

---

## 阶段 P1 · 服务层重构（预计 2 周）

### P1-SVC · 接口抽取

**P1-SVC-01** ✅ 为以下 Service 抽接口（放入 `Interfaces/`）
- ICsvImportService、IXmlImportService
- IIoTableImportService、IIoProgramGenerationService
- IGitPullService、IGeneratedArtifactSyncService
- INamingRulesService、IDesignerLayoutService、IDesignerProjectService
- IFlowLogCsvService、ITrendHistoryService

**P1-SVC-02** ✅ DI 注册改为 `AddSingleton<IXxx, Xxx>()`
- 同时保留具体类型注册作为兼容垫片：
  ```csharp
  services.AddSingleton(sp => (CsvImportService)sp.GetRequiredService<ICsvImportService>());
  ```
- 让现有按具体类型注入的 ViewModel 不需立即修改
- 验收：启动无 DI 解析错误

---

### P1-OPC · 拆分 OpcUaService（保持 IOpcUaService 不变）

**P1-OPC-01** ✅ 新建 `Services/OpcUa/` 子目录

**P1-OPC-02** ✅ 抽取 `OpcUaApplicationConfigurationFactory`（静态）
- 职责：构建 ApplicationConfiguration、ResolveApplicationRoot、PKI 目录创建
- 验收：连接 / 断开测试通过

**P1-OPC-03** ✅ 抽取 `OpcUaSessionFactory`（静态）
- 职责：CreateAsync（含 EndpointDiscovery）+ CreateDirectAsync（直连兜底）
- 验收：标准 + 兜底两条连接路径都能工作

**P1-OPC-04** ✅ 抽取 `OpcUaResolvedNodeCacheStore`（实例）
- 职责：opc-resolved-node-cache.json 的 Load/Save，含 SemaphoreSlim 串行化
- 提供 `NormalizeEndpointKey(string)` 静态方法
- 验收：缓存读写功能回归

**P1-OPC-05** ✅ OpcUaService 内部委托给 3 个新类，删除已搬走的方法
- 删除：BuildConfigurationAsync / ResolveApplicationRoot / CreateSessionAsync / CreateDirectSessionAsync / NormalizeEndpointKey / GetResolvedNodeCachePath / LoadResolvedNodeCacheAsync / SaveResolvedNodeCacheAsync / ResolvedNodeCacheFile
- 保留：BuildConnectionAttempts、ResolveNodeIdAsync 系列、订阅 / 读写
- 验收：OpcUaService 行数从 1145 → ~870；外部 IOpcUaService 不变

---

### P1-IO · 拆分 IoTableImportService（推迟与 P1-VM 一起）

**P1-IO-01** ✅ 定义 `IIoTableParser` 接口（Parse(IReadOnlyList) → IoTableParseResult）
- 已创建 `Interfaces/IIoTableParser.cs`、`Models/IoTableParseResult.cs`、`Services/IoTableParser.cs`
- `IoTableImportService` 已注入 `IIoTableParser`，解析逻辑与文件 I/O 分离
- IoTableImportService 从 1076 行缩减至 675 行

---

### P1-THR · 统一线程同步

**P1-THR-01** ✅ 编写 `docs/线程模型规范.md`
- 三类执行上下文、同步原语选型、async/await 约定、ObservableCollection 规则、Timer 生命周期、Code Review 清单

**P1-THR-02** ✅ 审计 MainViewModel 中 4 个 sync object
- 已发现：`_ioTableRowsSync` / `_generatedIoProgramsSync` / `_manualCylinderBlocksSync` / `_logsSync` 已正确用于 `BindingOperations.EnableCollectionSynchronization`，**符合规范**
- 其他 ObservableCollection：通过 `RunOnUiThreadAsync` marshal，符合规范
- 验收：无需调整

**P1-THR-03** ✅ OpcUaService 同步原语统一
- 已移除 Service 内 object lock 字段；订阅状态改用 `ConcurrentDictionary`
- OPC UA NodeId 缓存 IO 串行化已收敛到 `OpcUaResolvedNodeCacheStore`

---

## 阶段 P1 · MainViewModel 解耦（预计 2-3 周，**风险高，需用户 review 后启动**）

### P1-VM · 拆分 MainViewModel

**P1-VM-01** ✅ 新建目录 `ViewModels/Modules/`

**P1-VM-02** ✅ 抽取 `ManualControlViewModel`（命令+属性代理完成，共 17 命令 / 36 属性）
**P1-VM-03** ✅ 抽取 `MonitorPageViewModel`（1204 行）
**P1-VM-04** ✅ 抽取 `DesignerPageViewModel`（命令+属性+GitPull 代理完成，共 23 命令 / 70+ 属性）
**P1-VM-05** ✅ 抽取 `AlarmPageViewModel`（744 行）
**P1-VM-06** ✅ 抽取 `RecipePageViewModel`（278 行）
**P1-VM-07** ✅ 抽取 `ParameterPageViewModel`（109 行）
**P1-VM-08** ✅ 抽取 `GitPullViewModel`（独立模块 VM，3 命令 + 12 属性）
**P1-VM-09** ✅ 抽取 `RefreshCoordinator`（`Services/RefreshCoordinator.cs`，纯策略服务）
**P1-VM-10** ✅ 缩减 `MainViewModel`（GitPull 912→72 行，Recipe 已薄委托，Parameter 已薄委托）
- GitPull: 从 MainViewModel.GitPull.cs（912行）完全移入 GitPullViewModel（~830行含 InProShop）
- MainViewModel.GitPull.cs 现为 72 行委托层
- 竞态修复：`InitializeAsync` 先于 `SetGitPullViewModel` 时暂存 GitPullSettings
- `PersistConfigAsync` 改为 internal 以便模块 VM 访问
**P1-VM-11** ✅ DI 注册所有新 VM（`Bootstrapper.cs` + `MainWindowViewModel.cs`）

**⚠️ 重要**：盲拆 9835 行 UI 状态机风险极高，必须用户决定拆分粒度 / 模块边界 / 是否换 IMessenger 通讯，再启动

---

## 阶段 P2 · 测试体系（预计 3-4 周）

### P2-TEST · 测试基础设施

**P2-TEST-01** ✅ 新建 `tests/ApexHMI.Tests/` 项目
- 框架：xUnit、Moq、FluentAssertions、coverlet
- TargetFramework: net48（与主项目一致）
- ProjectReference 主项目
- 验收：`dotnet test` 可跑通空测试

**P2-TEST-02** ✅ ApexHMI.csproj 添加 `tests/**` 排除
- 防止 WPF 临时项目把 tests 文件吸进主项目编译
- 添加：
  ```xml
  <DefaultItemExcludes>$(DefaultItemExcludes);tests\**</DefaultItemExcludes>
  <Compile Remove="tests\**" />
  ```
- 验收：dotnet build 主项目时不再报 xunit 找不到

**P2-TEST-03** ✅ TestHelpers/TempDir.cs（using 包裹自动清理临时目录）

**P2-TEST-04** ✅ Services 层覆盖（目标 60%）
- 优先级：
  - SecretProtector（DPAPI round-trip）
  - ConfigurationService（加密/解密 + 旧明文迁移）
  - NamingRulesService（默认配置回写、归一化）
  - DesignerLayoutService / DesignerProjectService（JSON round-trip）
  - TrendHistoryService / FlowLogCsvService（CSV 追加 + 加载）
  - OpcUaResolvedNodeCacheStore（多 endpoint 隔离）
- 验收：每个服务至少 3 个用例，全部通过
- 已覆盖：SecretProtector、ConfigurationService、NamingRulesService、DesignerLayoutService、DesignerProjectService、TrendHistoryService、FlowLogCsvService、OpcUaResolvedNodeCacheStore

**P2-TEST-05** ✅ ViewModel 关键命令测试（依赖 P1-VM 完成）
- ManualViewModel（4 个测试：17 命令存在、命令独立性、集合委托、属性双向委托）
- DesignerViewModel（6 个测试：20 设计器命令、GitPull 命令、命令独立性、集合委托、模式标志、IO 属性委托）
- GitPullViewModel（12 个测试：命令存在、默认值、CanExecute 切换、设置恢复/序列化往返、文件夹路径解析等）
- 验收：61/61 测试全部通过

**P2-TEST-06** ✅ 配置 GitHub Actions / Azure Pipeline
- 触发：PR / push to main
- 步骤：build → test → coverage → 上传 artifact
- 已添加：`.github/workflows/dotnet-ci.yml`

---

## 阶段 P2 · UI / 性能（持续）

**P2-UI-01** ✅ MainWindow.xaml.cs 业务逻辑搬到 VM（依赖 P1-VM）
- ConnectButton/DisconnectButton → ParameterViewModel 命令绑定
- OpenCommunicationConfig → ParameterViewModel RelayCommand
- MainWindow.xaml.cs 死代码 handler 已删除（3 个无引用 handler）
- ParameterView.xaml.cs code-behind handler 已删除（3 个）
**P2-UI-02** ⏸ Converter 颜色集中到 `App.xaml` ResourceDictionary（等需要主题化时再做）
**P2-UI-03** ✅ Converters/RobotConverters.cs ConvertBack `throw NotImplementedException` → `Binding.DoNothing`
- 20 处 throw，可一次 `sed` 替换
- 验收：grep `NotImplementedException` 在 Converters 下为 0

**P2-UI-04** ✅ 大型 ObservableCollection 启用虚拟化
- IO 表、报警历史、Tag 列表
- 验收：1 万行数据滚动无卡顿

**P2-UI-05** ✅ DispatcherTimer 全项目审计 + Stop/Dispose

**P2-UI-06** ✅ OpcUaService _monitoredItems 生命周期审计

---

## 阶段 P2 · 配置整合

**P2-CFG-01** ✅ 合并 config/*.json → 单一 `appsettings.json` + 分节
**P2-CFG-02** ✅ 编写 JSON Schema (`appsettings.schema.json`)
**P2-CFG-03** ✅ 抽取 `AppOptions` 系列类，`IOptions<T>` 注入
**P2-CFG-04** ✅ 魔法数迁移：
- OpcUaService.cs:22-24（Timeout、Parallelism、Probe candidates）
- IoProgramGenerationService.cs:306,322（IP 地址）
- 验收：grep 上述数字出现位置仅在配置类

---

## 阶段 P3 · 工程化（持续）

**P3-I18N-01** ✅ 引入 ResX 资源文件（zh-CN / en-US）
**P3-I18N-02** ✅ XAML 字符串迁移（MainWindow + HomeView 已完成，其余页面依模式增量迁移）
**P3-CI-01** ✅ 集成 Roslynator + StyleCop.Analyzers
**P3-CI-02** ✅ PR 模板 + CHANGELOG.md
**P3-APM-01** ❌ 集成 Application Insights 或自建埋点（跳过，非当前需求）

---

## 已知缺陷（P2-TEST 抓出）

**BUG-CONFIG-01** ✅ ConfigurationService.LoadAsync 中 `DecryptSecrets` 调用位置错误
- 问题：原本在 Connection 加载后立即调用，导致 GitPull 加载完仍是密文
- 修复：移到所有 partition 加载完成后
- 测试用例：`ConfigurationServiceTests.SaveThenLoad_PreservesPlainPassword_AndOnDiskIsEncrypted`

**BUG-FLOWCSV-01** ✅ `FlowLogCsvService.LoadAsync` 不识别引号内换行
- 文件：`Services/FlowLogCsvService.cs:48`
- 现象：`AppendAsync` 写入含换行的字段时正确包入引号，但 `LoadAsync` 用 `File.ReadAllLines` 按物理行切，导致这种记录读回时丢失
- 修复方案：已实现支持多行字段的 CSV record reader
- 测试：`CsvHistoryServiceTests.FlowLogLoadAsyncParsesQuotedFieldsContainingNewLines`
- 优先级：P2

---

## 任务依赖关系

```
P0-LOG ─┬─→ P0-EXC
        └─→ P0-ASY  ─→ P1-VM ─┬─→ P2-TEST(VM)
P0-SEC ────────────────────────┘
P1-SVC ─→ P1-OPC ──────────────→ P2-TEST(Service)
P1-SVC ─→ P1-IO  ──────────────┘
P1-DOC (独立)
P2-CFG (完成)
P2-UI  (部分完成，依赖 P1-VM)
P3-CI  (完成) ✅
P3-I18N (完成) ✅
```

---

## 执行约定

1. **每个原子任务 / 阶段一次提交**：commit 信息格式 `[P0-LOG] 引入 Serilog 日志`
2. **每阶段完成必跑**：`dotnet build` + `dotnet test` + 手动启动一次 ApexHMI.exe
3. **新增代码必带 XML 注释**（public API）
4. **拆分类时**：原文件保留为 Facade 以避免破坏调用方，下个阶段再清理
5. **遇阻塞**：在本文件对应任务标记 ❌ 并写明原因
