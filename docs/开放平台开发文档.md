# ApexHMI 开放平台开发文档（Phase A / B / C）

> 本文档覆盖开放平台（Open HMI Platform）三个阶段的目标、设计与任务分解。
>
> - **Phase A — 运行时**：✅ 已完成（分支 `claude/exciting-montalcini-582697`，commit `a361337` + `cd66639`），尚未合并到 master。
> - **Phase B — 编辑器**：⬜ 待开发，依赖 Phase A 合并。
> - **Phase C — IO→HMI 一体化**：⬜ 待开发，依赖 Phase A、依赖现有 IoTableImportService。
>
> 关联文档：
> - V1 任务清单：[docs/开发任务清单.md](开发任务清单.md)
> - V2 任务清单：[docs/v2-开发任务清单.md](v2-开发任务清单.md)（V2-MERGE / V2-OPENPLAT 与本文对应）

---

## 0. 总览

### 0.1 愿景

**让客户的 HMI 界面成为一份 JSON 文件，而不是 C# 源码。**

修改界面不需要重新编译 ApexHMI.exe，不需要 Visual Studio，甚至不需要程序员 —— 改 `projects/<客户>/project.json`，重启或点"重载工程"即可生效。

### 0.2 业务背景

ApexHMI 为不同客户的产线提供 HMI 界面。每个客户的设备配置不同：
- 客户 A：3 气缸 + 2 伺服 + 产量看板
- 客户 B：1 机械手 + 6 传感器 + 报警面板
- 客户 C：纯监控页面

**当前做法（旧设计器）**：每来一个客户，工程师改 [DesignerView.xaml](../Views/Pages/DesignerView.xaml) 和 [MainViewModel.Designer.cs](../ViewModels/MainViewModel.Designer.cs)，重新编译发布**专属版本**。

**开放平台做法**：所有客户共用**同一个** ApexHMI.exe，每个客户拿到独立的 `project.json`。运行时由 `DynamicPageHost` 读 JSON 动态渲染。

### 0.3 阶段划分

| 阶段 | 目标 | 状态 |
|---|---|---|
| Phase A — 运行时 | 把 JSON 工程渲染成可运行的 HMI 页面 | ✅ |
| Phase B — 编辑器 | 不写 JSON，用图形界面增删改 page / widget / 属性 / 绑定 | ⬜ |
| Phase C — IO→HMI 一体化 | 从 IO 表自动生成对应控件页（气缸→气缸控件，伺服→伺服控件） | ⬜ |

### 0.4 术语表

| 术语 | 含义 |
|---|---|
| **工程（Project）** | 一份 `project.json` + 关联 assets，对应 `ProjectDocument` |
| **页面（Page）** | 一个 HMI 视图，对应 `PageDefinition`（含 RouteKey、画布大小、控件列表） |
| **控件（Widget）** | 页面上的一个 UI 元素，对应 `WidgetInstance`（类型 + 位置 + 属性 + 绑定 + 动作） |
| **TypeId** | 控件类型标识符（"text" / "bool-lamp" / "button" 等），由 `WidgetRegistry` 注册 |
| **TagId** | 逻辑变量名，对应 `tags.json` 中 `TagItem.Name`，与具体 OPC UA NodeId 解耦 |
| **RouteKey** | 页面路由键（"overview" / "manual"），按钮通过它跳转目标页 |

---

## 1. Phase A — 运行时（已完成）

### 1.1 总体架构

```
┌─────────────────── ApexHMI.exe ───────────────────┐
│                                                   │
│  RuntimeProjectService                            │
│       │ 加载 project.json                         │
│       ▼                                           │
│  ProjectDocument ─► PageDefinition ─► WidgetInstance │
│                                          │        │
│  DynamicPageHostViewModel ◄──────────────┘        │
│       │ 调 WidgetRegistry.Create(typeId)          │
│       ▼                                           │
│  四种内置控件：text / bool-lamp / numeric / button│
│       ▲                                           │
│       │ 值推送                                    │
│  RuntimeDataBindingService                        │
│       │ 订阅                                      │
│       ▼                                           │
│  IOpcUaService ─► PLC                             │
│                                                   │
│  DataPointCatalog ─► tags.json（TagId → NodeId）  │
└───────────────────────────────────────────────────┘
```

### 1.2 数据模型（[Models/RuntimeUi/](../Models/RuntimeUi/)）

```csharp
ProjectDocument                     // project.json 根对象
├── SchemaVersion : int             // 当前 = 1，由 ProjectMigration 升级
├── ProjectName : string
├── DefaultPageRouteKey : string?   // 启动默认页
└── Pages : List<PageDefinition>
        ├── Id / Title / RouteKey
        ├── RequiredRole : string?  // null = 所有人可见
        ├── CanvasWidth / CanvasHeight  (绝对定位画布)
        └── Widgets : List<WidgetInstance>
                ├── Id / TypeId
                ├── X / Y / Width / Height
                ├── Properties : Dictionary<string,string>
                ├── Binding : BindingSpec?
                │       ├── TagId           // 引用 TagItem.Name，不写死 NodeId
                │       ├── AccessMode      // Subscribe / ReadOnly / ReadWrite / WriteOnly
                │       └── DataType        // Bool / Int / Float / String
                ├── ActionType : string?    // "write-bool" / "write-pulse" / "navigate"
                └── ActionParam : string?   // 写入值 或 跳转目标 RouteKey
```

**设计要点**：
- `Properties` 是字符串字典，每种控件自行约定 key —— 牺牲强类型换可演化性（加新属性不破坏 JSON 兼容性）
- `Binding.TagId` 引用**逻辑名**而非 NodeId，因此换 PLC / 换地址不影响工程文件
- `SchemaVersion` 配合 `ProjectMigration.Migrate()` 支持读时升级，老工程文件可被新版本无损打开

### 1.3 服务层

#### RuntimeProjectService（[`Services/RuntimeProjectService.cs`](.claude/worktrees/exciting-montalcini-582697/Services/RuntimeProjectService.cs)）
- `LoadDefault()` — 加载 `<exe>/projects/_sample/project.json`，不存在时自动生成两页演示工程
- `Load(path)` — 反序列化 + 调用 `ProjectMigration.Migrate`
- `Save(doc, path)` — 序列化为缩进 JSON，UTF-8 编码

#### WidgetRegistry（[`Services/RuntimeUi/WidgetRegistry.cs`](.claude/worktrees/exciting-montalcini-582697/Services/RuntimeUi/WidgetRegistry.cs)）
控件类型 → 工厂委托的注册表：
```csharp
Register("text",             (m, ctx) => CreateView(new TextWidgetViewModel(m, ctx),             new TextWidget()));
Register("bool-lamp",        (m, ctx) => CreateView(new BoolLampWidgetViewModel(m, ctx),        new BoolLampWidget()));
Register("numeric-readonly", (m, ctx) => CreateView(new NumericReadonlyWidgetViewModel(m, ctx), new NumericReadonlyWidget()));
Register("button",           (m, ctx) => CreateView(new ButtonWidgetViewModel(m, ctx),          new ButtonWidget()));
```
未知 TypeId 返回灰色占位 `[未知控件]`，不会让整页崩溃。

#### DataPointCatalog（[`Services/DataBinding/DataPointCatalog.cs`](.claude/worktrees/exciting-montalcini-582697/Services/DataBinding/DataPointCatalog.cs)）
- 启动时从 `config/tags.json` 加载 `TagItem` 列表
- `Merge(IEnumerable<TagItem>)` 接受外部刷新（如 IO 表导入后）
- `FindTag(tagId)` 查表

#### RuntimeDataBindingService（[`Services/DataBinding/RuntimeDataBindingService.cs`](.claude/worktrees/exciting-montalcini-582697/Services/DataBinding/RuntimeDataBindingService.cs)）
- `AttachAsync(host)` — 切换页面时为页面中的 TagId 建立 OPC UA 订阅，已订阅过的不重复
- `WriteAsync(tagId, value)` — 主动写值
- 监听 `IOpcUaService.TagValueChanged` 事件，回调 `host.PushTagValue` 推到对应 widget

### 1.4 ViewModel 层（[`ViewModels/Runtime/`](.claude/worktrees/exciting-montalcini-582697/ViewModels/Runtime/)）

**WidgetViewModelBase** — 各 Widget VM 基类，提供 `RegisterValueCallback` 抽象访问。

四个具体 VM：
- `TextWidgetViewModel` — 静态文本（不订阅）
- `BoolLampWidgetViewModel` — Bool 指示灯，按 trueColor/falseColor 切换
- `NumericReadonlyWidgetViewModel` — 数显（含 unit / format）
- `ButtonWidgetViewModel` — 按钮，点击触发 `IWidgetDataContext.ExecuteAction(ActionType, ActionParam)`

**DynamicPageHostViewModel** — 页面宿主：
- 持有当前 `PageDefinition`
- 调 `IWidgetViewFactory.Create()` 实例化每个控件视图，按 `X/Y` 包成 `PositionedWidget`
- 实现 `IWidgetDataContext`：维护 `tagId → List<Action<string>>` 回调表，`PushTagValue(tagId, value)` 时分发
- `BoundTagIds` 暴露当前页所有需订阅的 TagId（供 `RuntimeDataBindingService` 使用）

### 1.5 视图层（[`Views/Runtime/`](.claude/worktrees/exciting-montalcini-582697/Views/Runtime/)）

- `DynamicPageHost.xaml` — `Canvas` 宿主，`ItemsControl` 绑定 `WidgetElements`，每项按 `X/Y` 设置 `Canvas.Left/Top`
- `Widgets/TextWidget.xaml` / `BoolLampWidget.xaml` / `NumericReadonlyWidget.xaml` / `ButtonWidget.xaml` — 四个独立 UserControl

页面入口 [`Views/Pages/RuntimeDesignerView.xaml`](.claude/worktrees/exciting-montalcini-582697/Views/Pages/RuntimeDesignerView.xaml) 改造为：
- 顶部：页面下拉选择 + "重载工程"按钮
- 中部：嵌入 `DynamicPageHost`

### 1.6 集成点

#### Bootstrapper（[`Bootstrapper.cs`](.claude/worktrees/exciting-montalcini-582697/Bootstrapper.cs)）新增四项 DI 注册：
```csharp
services.AddSingleton<IDataPointCatalog, DataPointCatalog>();
services.AddSingleton<IWidgetViewFactory, WidgetRegistry>();
services.AddSingleton<RuntimeProjectService>();
services.AddSingleton<RuntimeDataBindingService>();
```

#### MainWindowViewModel 改动
- 构造注入新四服务
- `InitializeDynamicRuntime()` 加载默认工程并 `AttachAsync` 当前页面
- 路由动作处理器：`navigate` → 切页；`write-bool` → 解析 `"TagId|True"` 写值；`write-pulse` → 写 True 后延时写 False
- `ReloadRuntimeProjectCommand` — 热重载

#### ApexHMI.csproj
- 添加 `<Content Include="projects\**\*.*" CopyToOutputDirectory="PreserveNewest" />` 让示例工程进入输出

### 1.7 演示工程（首次启动自动生成）

`projects/_sample/project.json` 含两页：

**页面 1：运行总览**（RouteKey=`overview`）
- 标题 "设备状态总览"
- 指示灯 "系统就绪" → `SystemReady`
- 指示灯 "运行中" → `Running`
- 数显 "产量" → `ProductCount`
- 数显 "节拍" → `CycleTime`
- 按钮 "前往手动页" → `navigate manual`

**页面 2：手动操作**（RouteKey=`manual`）
- 指示灯 "气缸1 到位" → `Cyl1_FwdSensor`
- 按钮 "气缸1 前进" → `write-bool Cyl1_FwdCmd|True`
- 按钮 "气缸1 退回" → `write-bool Cyl1_FwdCmd|False`
- 按钮 "返回总览" → `navigate overview`

### 1.8 已交付能力清单

| 能力 | 状态 | 实现位置 |
|---|---|---|
| 工程文件 JSON 序列化 / 反序列化 | ✅ | RuntimeProjectService |
| 读时迁移 (SchemaVersion) | ✅ | ProjectMigration |
| 控件类型注册 / 工厂模式 | ✅ | WidgetRegistry / IWidgetViewFactory |
| 4 种内置控件 | ✅ | text / bool-lamp / numeric-readonly / button |
| 未知控件 fallback 占位 | ✅ | WidgetRegistry.CreateFallback |
| 逻辑 TagId → 物理 NodeId 解耦 | ✅ | DataPointCatalog |
| OPC UA 订阅 + 值推送 | ✅ | RuntimeDataBindingService |
| Subscribe / ReadOnly / ReadWrite / WriteOnly 四种模式 | 🟡 模型已定义，运行时只用了 Subscribe + 写 | BindingSpec |
| 页面跳转动作 (navigate) | ✅ | DynamicPageHostViewModel |
| 写 Bool 值 (write-bool) | ✅ | MainWindowViewModel 路由 |
| 写脉冲 (write-pulse) | ✅ | MainWindowViewModel 路由 |
| 角色权限模型位 | 🟡 模型已定义 (`PageDefinition.RequiredRole`)，但运行时**未实现拦截** | PageDefinition |
| 热重载 | ✅ | ReloadRuntimeProjectCommand |
| 页面下拉选择 | ✅ | RuntimeDesignerView |

### 1.9 已知限制（Phase A 未做、留给 B/C 或后续阶段）

- ❌ **没有可视化编辑器**：JSON 必须手写或脚本生成（→ Phase B）
- ❌ **控件库小**：仅 4 种基础控件，缺 气缸 / 机械手 / 报警条 / 趋势图 / IO 表 / Recipe 表（→ Phase B 增量补，每种 widget 1 个 issue）
- ❌ **无逻辑编排**：按钮只能写一个值或跳一页，做不了"按下后写一组值并等 1s 再读回"（→ 后续阶段，可能 Phase D）
- ❌ **assets 不解析**：图片/图标不能由工程引用（→ Phase B 顺带）
- ❌ **主题不可覆盖**：颜色硬编码在 properties，不能定义工程级主题（→ Phase B 顺带）
- ❌ **角色拦截未实现**：`RequiredRole` 字段存在但运行时未校验（→ Phase B-04）
- ❌ **无单元测试**：33 个文件 0 测试（→ V2-MERGE 合并后必须补）
- ❌ **未接入 V1 治理基础设施**：`DataPointCatalog.TryLoadFromConfigFile` 用空 catch、未用 Serilog；不用 IOptions（→ 合并时随手修）

---

## 2. Phase B — 编辑器（待开发）

### 2.1 目标

让运营 / 现场工程师**不接触 JSON** 也能改 HMI：
- 图形化新建 / 重命名 / 重排 / 删除页面
- 图形化新增 / 移动 / 改属性 / 改绑定 / 删除控件
- 改完即时预览（同进程切换"设计模式 ↔ 运行模式"）
- 保存到 `project.json`

**非目标**（Phase B 不做）：
- 拖拽对齐 / 网格吸附 / 群组对齐（Phase B+）
- 撤销 / 重做（Phase B+，但 B 阶段为之留好接口）
- 多人协作 / 版本控制集成（永远不做，靠 git）

### 2.2 任务分解

| ID | 任务 | 输入 | 输出 | 验收 |
|---|---|---|---|---|
| **B-01** | `Services/RuntimeUi/IProjectEditorService` + 实现 | 当前 `ProjectDocument` | API: `AddPage / RemovePage / DuplicatePage / ReorderPages / RenamePage` | 单元测试覆盖每个方法 ≥ 2 用例（含异常路径） |
| **B-02** | 删页前引用检查 | `RemovePage(id)` | 扫描所有 `Widget.ActionType=="navigate"` 的 `ActionParam`，若指向被删页则阻止 + 返回错误 | 测试：删一个被引用的页报错；改 ActionParam 后能删 |
| **B-03** | `Services/RuntimeUi/IWidgetEditorService` + 实现 | 当前 `PageDefinition` | API: `AddWidget(typeId, x, y) / RemoveWidget(id) / UpdateProperty(id, key, value) / UpdateBinding(id, BindingSpec?) / MoveWidget(id, x, y) / ResizeWidget(id, w, h)` | 单元测试覆盖每个方法 |
| **B-04** | 角色拦截运行时实现 | `PageDefinition.RequiredRole` + `MainViewModel.CurrentUserRole` | 用户角色不足时：页面下拉隐藏该页 / 直接 navigate 时弹"权限不足"并阻止；编辑模式仅 Engineer+ 可见 | 测试：模拟 Operator 角色无法进编辑器 |
| **B-05** | 简易设计时面板 UI | DynamicPageHost + ProjectEditorService + WidgetEditorService | `Views/Pages/RuntimeProjectEditorView.xaml`：左侧页面树 + 右侧属性网格 + 中央预览（即 DynamicPageHost），不做拖拽 | 能完整完成"加页 → 加按钮 → 设 X/Y → 设 Properties → 设 Binding → 保存 → 重载验证" |
| **B-06** | 属性网格自适应 | `WidgetInstance.Properties: Dictionary<string,string>` | 根据 TypeId 提示该控件支持哪些 key，未知 key 也允许编辑（自由扩展） | 设新 key 不丢失 |
| **B-07** | 绑定选择器 | `DataPointCatalog.GetAll()` | 控件 Binding 可从 Tag 列表中筛选选择，支持搜索 | Tag 数百个时仍可用 |
| **B-08** | 工程级 assets/ 与主题 | `projects/<name>/assets/*.png` + `projects/<name>/theme.json` | 控件 Properties 支持 `image=assets/logo.png`；主题文件覆盖颜色 token | TextWidget 显示主题色；ButtonWidget 引用 assets 图标 |
| **B-09** | 撤销 / 重做接口位 | IProjectEditorService / IWidgetEditorService | 每个方法返回 `IRevertibleEdit`（命令模式），但 B 阶段只实现 Apply，Undo 留 NotImplementedException | 接口存在；后续阶段补实现不破坏 API |
| **B-10** | 控件库扩充（首批） | 业务需求 | 新增 `cylinder` / `axis` / `alarm-banner` 三种控件类型 | 演示工程升级到 schemaVersion 2，补迁移 |

### 2.3 关键设计决策

#### B-D1 编辑模型 vs 运行模型
**决策**：保持单一模型 `ProjectDocument`。编辑器只是读写它的服务，不引入"编辑器专用模型"。
**理由**：避免双模型同步成本；改完即时预览。

#### B-D2 数据流方向
```
[属性网格] ──UpdateProperty──► [WidgetEditorService] ──修改─► [WidgetInstance]
                                                                  │
                                                                  ▼
                              [DynamicPageHostViewModel] ◄──── PropertyChanged
                                       │
                                       ▼
                                  [Widget VM 重读]
```
**决策**：`WidgetInstance` 升级为 `ObservableObject`（CommunityToolkit.Mvvm）。
**理由**：让属性面板改完即时反映到画布。

#### B-D3 保存策略
**决策**：自动保存（每次编辑 1.5s debounce 后写盘），并保留显式"保存"按钮。
**理由**：现场不易记得手动保存；但保留按钮供仪式感。

#### B-D4 设计 / 运行模式开关
**决策**：复用 V1 已有的 `MainViewModel.IsRuntimeMode`，运行 / 设计两个模式只切显示和命令可用性，不切页。
**理由**：避免双窗口；改完立即在同一 Canvas 上点按钮验证。

### 2.4 验收标准（端到端）

1. 工程师 A 在新机器上启动 ApexHMI.exe，进入开放平台标签页
2. 切到设计模式
3. 新建页面 "报警概览"，加 1 个 alarm-banner 控件（绑定 `Alarm_System_Active`）
4. 点保存 → `projects/_sample/project.json` 增加该页
5. 切到运行模式 → 新页面立即出现在下拉
6. 关闭 ApexHMI.exe，重启 → 新页面仍在
7. 全过程未碰任何 JSON / 任何源码

---

## 3. Phase C — IO→HMI 一体化（待开发）

### 3.1 目标

把现有 [IoTableImportService](../Services/IoTableImportService.cs) 导入的 IO 表（含气缸 / 伺服 / 传感器 / 输出），**一键生成**对应的开放平台工程页面。

#### 现状问题
- IO 表导入后只生成了 PLC 程序（IoProgramGenerationService），**没有自动生成 HMI 页面**
- 工程师还要手写 JSON：每个气缸要 3-5 个 widget（指示灯 + 前进按钮 + 退回按钮 + 互锁灯）
- 出错率高、效率低

### 3.2 工作流目标

```
                  IoTable.csv
                       │
                       ▼
            IoTableImportService（已有）
                       │
                       ▼
              IoTableParseResult（已有）
                       │
                       ▼
          ┌────────────┴────────────┐
          ▼                         ▼
  IoProgramGenerationService   HmiGenerationService（Phase C 新增）
  （PLC .st 文件，已有）         │
                                ▼
                         ProjectDocument 增量
                                │
                                ▼
                       IProjectEditorService.MergeFrom(...)
```

### 3.3 任务分解

| ID | 任务 | 输入 | 输出 | 验收 |
|---|---|---|---|---|
| **C-01** | `Services/Generation/IHmiGenerationRule` | IoTableRow / 块类型 | 生成一个 `IEnumerable<WidgetInstance>` + 可选页 | 每条 rule 单元测试 |
| **C-02** | 内置规则：气缸块 → 6 个控件 | ManualCylinderBlockItem | 标题 / 到位灯 / 前进按钮 / 退回按钮 / 互锁灯 / 错误灯 | 演示 IO 表导入后气缸页正确生成 |
| **C-03** | 内置规则：伺服块 → ? 个控件 | ManualAxisBlockItem | 当前位置数显 / 速度数显 / 启动 / 停止 / 复位 | 同上 |
| **C-04** | 内置规则：纯 IO 点 → 灯 + 按钮 | IoTableRow（输入或输出） | bool-lamp 或 button | 同上 |
| **C-05** | 自动布局算法 | 控件列表 | X/Y 自动排版（grid / pack / 流式），避免重叠 | 1000 个控件分页生成无重叠 |
| **C-06** | 命名归一化 | RuleSet | 沿用 NamingRulesService，使 TagId 与 IO 表一致 | TagId 解析正确 |
| **C-07** | `IProjectEditorService.MergeFrom(generatedDoc, MergeStrategy)` | 现有 + 生成 | 三种策略：覆盖 / 追加新增 / 仅补缺失 | 测试三种策略 |
| **C-08** | 一键命令 UI | DesignerViewModel | "从当前 IO 表生成 HMI 页面"按钮 | 端到端：导 IO 表 → 点按钮 → 工程文件已增加对应页 |
| **C-09** | 增量再生成保留手动改动 | 二次生成时，已被人工改过 X/Y/Properties 的控件不被覆盖 | 引入 `WidgetInstance.GeneratedFingerprint` | 测试：修改后再生成不丢失改动 |

### 3.4 关键设计决策

#### C-D1 生成 vs 模板
**决策**：用代码规则（IHmiGenerationRule 实现）而不是 XAML / mustache 模板。
**理由**：HMI widget 间的相对位置约束、命名规则等用代码表达更清晰；模板适合纯文本生成。

#### C-D2 一次性 vs 增量
**决策**：增量生成（C-09），通过 fingerprint 区分"机器写的"vs"人工改的"。
**理由**：现场实际场景就是改了 IO 表后追加生成几个控件，不应清掉之前的人工调整。

#### C-D3 与 PLC 程序生成的关系
**决策**：HmiGenerationService 与 IoProgramGenerationService **平行**，都消费 IoTableParseResult。
**理由**：解耦；某些客户可能只要 HMI 不要 PLC 代码（监控屏）。

### 3.5 验收标准（端到端）

1. 工程师导入一份含 5 个气缸的 IO 表
2. 点 "从 IO 表生成 HMI 页面"
3. project.json 中出现新页 "气缸控制（自动生成）"，含 30 个控件（5 × 6）正确排版
4. 切到运行模式，所有 widget 已绑定到正确的 OPC UA Tag，能写值能读回
5. 工程师手动调整其中一个气缸的按钮 X 坐标，保存
6. 再次导入更新过的 IO 表（多了 1 个气缸）+ 点生成
7. 已存在的 5 个气缸保留人工改动，新增第 6 个气缸自动生成

---

## 4. 路线图与依赖

```
V1 治理（已完成 master）
       │
       ▼
V2-MERGE-01：合并 Phase A 到 master
       │   ├── 解决 Bootstrapper.cs / MainWindowViewModel.cs / RuntimeDesignerView.xaml 冲突
       │   ├── 接入 Serilog（替换 DataPointCatalog 空 catch）
       │   ├── 接入 IOptions / IOpcUaService
       │   └── 补 Phase A 单元测试
       │
       ▼
Phase B（按 B-01 → B-10 顺序）
       │   并行可：B-04（角色拦截）、B-08（assets/主题）独立
       │
       ▼
Phase C（依赖 Phase B 的 IProjectEditorService.MergeFrom）
       │   并行可：C-02 / C-03 / C-04 三类规则独立
       │
       ▼
Phase B+ / 后续：拖拽 / 撤销重做 / 控件市场 / 工程导入导出包
```

### 4.1 关键依赖

| 后置任务 | 前置依赖 | 原因 |
|---|---|---|
| Phase B 全部 | V2-MERGE-01 | 编辑的对象（ProjectDocument）必须在 master |
| B-05 设计时面板 | B-01 + B-03 | 面板调用编辑器服务 |
| B-10 新控件 | Phase A 合并 | 新控件要 Register 到 WidgetRegistry |
| Phase C 全部 | Phase B 的 B-01（IProjectEditorService.MergeFrom） | 生成结果要"合并"到现有工程 |
| C-09 增量生成 | C-01 ~ C-04 已稳定 | 否则 fingerprint 频繁变 |

### 4.2 估算

| 阶段 | 工时 | 依赖等待 |
|---|---|---|
| V2-MERGE-01 | 0.5 天 | — |
| Phase B（B-01~B-10） | 2-3 周（1 人）| 无 |
| Phase C（C-01~C-09） | 2 周（1 人） | Phase B B-01 完成 |

---

## 5. 文件清单

### 5.1 Phase A 已落地（共 33 个新文件）

#### 数据模型
- [Models/RuntimeUi/ProjectDocument.cs](.claude/worktrees/exciting-montalcini-582697/Models/RuntimeUi/ProjectDocument.cs)
- [Models/RuntimeUi/PageDefinition.cs](.claude/worktrees/exciting-montalcini-582697/Models/RuntimeUi/PageDefinition.cs)
- [Models/RuntimeUi/WidgetInstance.cs](.claude/worktrees/exciting-montalcini-582697/Models/RuntimeUi/WidgetInstance.cs)
- [Models/RuntimeUi/BindingSpec.cs](.claude/worktrees/exciting-montalcini-582697/Models/RuntimeUi/BindingSpec.cs)
- [Models/RuntimeUi/ProjectMigration.cs](.claude/worktrees/exciting-montalcini-582697/Models/RuntimeUi/ProjectMigration.cs)

#### 服务层
- [Services/RuntimeProjectService.cs](.claude/worktrees/exciting-montalcini-582697/Services/RuntimeProjectService.cs)
- [Services/RuntimeUi/IWidgetViewFactory.cs](.claude/worktrees/exciting-montalcini-582697/Services/RuntimeUi/IWidgetViewFactory.cs)
- [Services/RuntimeUi/WidgetRegistry.cs](.claude/worktrees/exciting-montalcini-582697/Services/RuntimeUi/WidgetRegistry.cs)
- [Services/DataBinding/IDataPointCatalog.cs](.claude/worktrees/exciting-montalcini-582697/Services/DataBinding/IDataPointCatalog.cs)
- [Services/DataBinding/DataPointCatalog.cs](.claude/worktrees/exciting-montalcini-582697/Services/DataBinding/DataPointCatalog.cs)
- [Services/DataBinding/RuntimeDataBindingService.cs](.claude/worktrees/exciting-montalcini-582697/Services/DataBinding/RuntimeDataBindingService.cs)

#### ViewModel 层
- ViewModels/Runtime/WidgetViewModelBase.cs
- ViewModels/Runtime/TextWidgetViewModel.cs
- ViewModels/Runtime/BoolLampWidgetViewModel.cs
- ViewModels/Runtime/NumericReadonlyWidgetViewModel.cs
- ViewModels/Runtime/ButtonWidgetViewModel.cs
- ViewModels/Runtime/DynamicPageHostViewModel.cs

#### 视图层
- Views/Runtime/DynamicPageHost.xaml(.cs)
- Views/Runtime/Widgets/TextWidget.xaml(.cs)
- Views/Runtime/Widgets/BoolLampWidget.xaml(.cs)
- Views/Runtime/Widgets/NumericReadonlyWidget.xaml(.cs)
- Views/Runtime/Widgets/ButtonWidget.xaml(.cs)

#### 修改
- ApexHMI.csproj（projects/** 复制输出）
- Bootstrapper.cs（+4 服务注册）
- ViewModels/Shell/MainWindowViewModel.cs（+121 行）
- Views/MainWindow.xaml（导航条目）
- Views/Pages/RuntimeDesignerView.xaml(.cs)（替换为动态宿主）

### 5.2 Phase B 待开发（规划）

```
Models/RuntimeUi/
├── ProjectMigration.cs               (升级到 v2，加 widget 默认 fingerprint)
└── (WidgetInstance.cs 改为 ObservableObject)

Services/RuntimeUi/
├── IProjectEditorService.cs          (B-01)
├── ProjectEditorService.cs           (B-01 / B-02)
├── IWidgetEditorService.cs           (B-03)
├── WidgetEditorService.cs            (B-03 / B-06)
├── IRevertibleEdit.cs                (B-09 接口位)
└── RoleBasedAccessGuard.cs           (B-04)

ViewModels/Runtime/Editor/
├── PageTreeViewModel.cs              (B-05)
├── PropertyGridViewModel.cs          (B-05 / B-06)
├── BindingPickerViewModel.cs         (B-07)
└── ProjectEditorViewModel.cs         (B-05 总宿主)

Views/Pages/
└── RuntimeProjectEditorView.xaml(.cs) (B-05)

Views/Runtime/Widgets/
├── CylinderWidget.xaml(.cs)          (B-10)
├── AxisWidget.xaml(.cs)              (B-10)
└── AlarmBannerWidget.xaml(.cs)       (B-10)
```

### 5.3 Phase C 待开发（规划）

```
Services/Generation/
├── IHmiGenerationRule.cs             (C-01)
├── HmiGenerationService.cs           (C-01 总入口)
├── Rules/
│   ├── CylinderHmiRule.cs            (C-02)
│   ├── AxisHmiRule.cs                (C-03)
│   └── IoPointHmiRule.cs             (C-04)
└── Layout/
    └── GridFlowLayoutEngine.cs       (C-05)

Models/RuntimeUi/
└── WidgetInstance.cs                  (+ GeneratedFingerprint, C-09)
```

---

## 6. 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| Phase A 没有单元测试 | 后续重构难发现回归 | V2-MERGE-01 合入时强制补测试 |
| `Properties` 字典是字符串 → 字符串 | 类型错误运行时才暴露 | B-06 属性网格做类型校验，警告非法值 |
| 工程文件膨胀（数千控件） | 加载慢、JSON 难审 | C-05 分页 + 工程文件按页拆分（Phase B+） |
| OPC UA Tag 改名后所有工程 broken | 客户工程失效 | 提供 "Tag 重定向"工具（B-07 顺带） |
| 角色拦截绕过（直接改 JSON） | 安全风险 | B-04 在加载层而非 UI 层校验 |
| 自动生成覆盖人工改动 | 现场改动丢失 | C-09 fingerprint 隔离 |

---

## 7. 不在本文档范围

- 持续集成 / 部署流水线 → V1 P3-CI / V2-LINT
- 国际化（控件 label 多语言）→ V2-I18N
- 性能基准 → V2-PERF
- 集成测试（OPC UA 模拟器）→ V2-INT

---

## 附：示例 project.json（节选）

```json
{
  "schemaVersion": 1,
  "projectName": "演示工程",
  "defaultPageRouteKey": "overview",
  "pages": [
    {
      "id": "page-overview",
      "title": "运行总览",
      "routeKey": "overview",
      "canvasWidth": 1280,
      "canvasHeight": 720,
      "widgets": [
        {
          "id": "w-title",
          "typeId": "text",
          "x": 40, "y": 40, "width": 300, "height": 40,
          "properties": {
            "text": "设备状态总览",
            "fontSize": "22",
            "foreground": "#0F172A"
          }
        },
        {
          "id": "w-lamp1",
          "typeId": "bool-lamp",
          "x": 40, "y": 100, "width": 160, "height": 32,
          "properties": {
            "label": "系统就绪",
            "trueColor": "#22C55E",
            "falseColor": "#EF4444"
          },
          "binding": {
            "tagId": "SystemReady",
            "accessMode": "Subscribe",
            "dataType": "Bool"
          }
        },
        {
          "id": "w-btn-go-manual",
          "typeId": "button",
          "x": 40, "y": 300, "width": 120, "height": 40,
          "properties": {
            "text": "前往手动页",
            "background": "#374151",
            "foreground": "#FFFFFF"
          },
          "actionType": "navigate",
          "actionParam": "manual"
        }
      ]
    }
  ]
}
```
