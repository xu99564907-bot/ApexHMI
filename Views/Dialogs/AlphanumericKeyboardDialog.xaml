<Window x:Class="ApexHMI.Views.Dialogs.AlphanumericKeyboardDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="键盘" Height="360" Width="720"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Background="#F8FAFC">
    <Window.Resources>
        <Style TargetType="Button" x:Key="KeyBtn">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="MinWidth" Value="48"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" x:Name="PreviewBox"
                 Height="40" FontSize="18" Padding="6,4"
                 VerticalContentAlignment="Center"
                 Background="#FFFFFF" BorderBrush="#CBD5E1"/>

        <ItemsControl Grid.Row="1" x:Name="KeyRows" Margin="0,8,0,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,6,0,0">
            <Button Content="Shift" Style="{StaticResource KeyBtn}" Click="OnShift" MinWidth="80"/>
            <Button Content="Esc" Style="{StaticResource KeyBtn}" Click="OnCancel" MinWidth="80"
                    Background="#FEE2E2" BorderBrush="#FCA5A5"/>
            <Button Content="Enter" Style="{StaticResource KeyBtn}" Click="OnEnter" MinWidth="120"
                    Background="#2563EB" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
