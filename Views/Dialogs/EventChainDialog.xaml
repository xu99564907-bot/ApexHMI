<Window x:Class="ApexHMI.Views.Dialogs.EventChainDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="事件链关联视图" Height="540" Width="780"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        Background="{DynamicResource Brush.SurfaceAlt}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock FontSize="16" FontWeight="Bold" Foreground="{DynamicResource Brush.Text}">
                <Run Text="关联关键字："/>
                <Run Text="{Binding Keyword}"/>
            </TextBlock>
            <TextBlock FontSize="12" Foreground="{DynamicResource Brush.TextMuted}" Margin="0,4,0,0"
                       Text="按时间顺序串联：审计动作（蓝） + 报警历史（红），覆盖 触发 → 确认 → 复位 → 恢复 整条链。"
                       TextWrapping="Wrap"/>
        </StackPanel>
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Entries}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  RowBackground="White" AlternatingRowBackground="#F1F5F9"
                  BorderBrush="#CBD5E1" BorderThickness="1">
            <DataGrid.Resources>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Source}" Value="审计">
                            <Setter Property="Foreground" Value="#1D4ED8"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Source}" Value="报警">
                            <Setter Property="Foreground" Value="#B91C1C"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Binding="{Binding Time, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="*"/>
                <DataGridTextColumn Header="来源" Binding="{Binding Source}" Width="80"/>
                <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="详情" Binding="{Binding Detail}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="关闭" IsCancel="True" Width="80" Height="32"
                    Background="#64748B" Foreground="White" BorderBrush="#64748B"/>
        </StackPanel>
    </Grid>
</Window>
