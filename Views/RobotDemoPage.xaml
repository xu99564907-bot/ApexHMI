<Page x:Class="ApexHMI.Views.RobotDemoPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:controls="clr-namespace:ApexHMI.Views.Controls"
      xmlns:vm="clr-namespace:ApexHMI.ViewModels"
      mc:Ignorable="d"
      Title="机械手控制演示">

    <Page.Resources>
        <Style x:Key="PageTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="PageSubtitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="Margin" Value="0,0,0,24"/>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="32">
            <!-- 页面标题 -->
            <TextBlock Text="EPSON机械手控制界面" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="浅色工业风格 - 基于XML结构体定义" Style="{StaticResource PageSubtitle}"/>

            <!-- 机械手1 - 就绪状态 -->
            <controls:RobotControl DataContext="{Binding Robot1}"/>

            <!-- 机械手2 - 运行状态 -->
            <controls:RobotControl DataContext="{Binding Robot2}"/>

            <!-- 机械手3 - 错误状态 -->
            <controls:RobotControl DataContext="{Binding Robot3}"/>
        </StackPanel>
    </ScrollViewer>
</Page>
