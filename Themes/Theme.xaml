<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== 语义调色板 (Phase 3) =====
         保留 Phase 2 各页面所有 hex 色值，只是提供 Brush 别名供后续逐步替换为语义引用：
         <Border Background="{DynamicResource Surface.Card}"/>
         注意：DynamicResource 才能支持后续主题切换；目前先用 StaticResource 即可。 -->

    <!-- 基础色 -->
    <Color x:Key="Color.Primary">#2563EB</Color>
    <Color x:Key="Color.PrimaryHover">#1D4ED8</Color>
    <Color x:Key="Color.PrimaryLight">#DBEAFE</Color>
    <Color x:Key="Color.Success">#0F766E</Color>
    <Color x:Key="Color.SuccessLight">#D1FAE5</Color>
    <Color x:Key="Color.Warning">#F59E0B</Color>
    <Color x:Key="Color.WarningLight">#FEF3C7</Color>
    <Color x:Key="Color.Danger">#DC2626</Color>
    <Color x:Key="Color.DangerHover">#B91C1C</Color>
    <Color x:Key="Color.DangerLight">#FEE2E2</Color>
    <Color x:Key="Color.Info">#7C3AED</Color>
    <Color x:Key="Color.InfoLight">#EDE9FE</Color>

    <!-- 文字 / 边框 / 背景 -->
    <Color x:Key="Color.Text">#0F172A</Color>
    <Color x:Key="Color.TextSecondary">#475569</Color>
    <Color x:Key="Color.TextMuted">#64748B</Color>
    <Color x:Key="Color.TextPlaceholder">#94A3B8</Color>
    <Color x:Key="Color.Border">#D7DFEA</Color>
    <Color x:Key="Color.BorderSoft">#E2E8F0</Color>
    <Color x:Key="Color.Surface">#FFFFFF</Color>
    <Color x:Key="Color.SurfaceAlt">#F8FAFC</Color>
    <Color x:Key="Color.SurfaceMuted">#F1F5F9</Color>
    <Color x:Key="Color.Background">#EEF2F7</Color>
    <Color x:Key="Color.HeaderBackground">#EEF2F7</Color>
    <Color x:Key="Color.HeaderText">#334155</Color>

    <!-- Brush 别名（供 Setter Property=... Value="{StaticResource ...}" 引用） -->
    <SolidColorBrush x:Key="Brush.Primary" Color="{StaticResource Color.Primary}"/>
    <SolidColorBrush x:Key="Brush.PrimaryHover" Color="{StaticResource Color.PrimaryHover}"/>
    <SolidColorBrush x:Key="Brush.PrimaryLight" Color="{StaticResource Color.PrimaryLight}"/>
    <SolidColorBrush x:Key="Brush.Success" Color="{StaticResource Color.Success}"/>
    <SolidColorBrush x:Key="Brush.SuccessLight" Color="{StaticResource Color.SuccessLight}"/>
    <SolidColorBrush x:Key="Brush.Warning" Color="{StaticResource Color.Warning}"/>
    <SolidColorBrush x:Key="Brush.WarningLight" Color="{StaticResource Color.WarningLight}"/>
    <SolidColorBrush x:Key="Brush.Danger" Color="{StaticResource Color.Danger}"/>
    <SolidColorBrush x:Key="Brush.DangerHover" Color="{StaticResource Color.DangerHover}"/>
    <SolidColorBrush x:Key="Brush.DangerLight" Color="{StaticResource Color.DangerLight}"/>
    <SolidColorBrush x:Key="Brush.Info" Color="{StaticResource Color.Info}"/>
    <SolidColorBrush x:Key="Brush.InfoLight" Color="{StaticResource Color.InfoLight}"/>

    <SolidColorBrush x:Key="Brush.Text" Color="{StaticResource Color.Text}"/>
    <SolidColorBrush x:Key="Brush.TextSecondary" Color="{StaticResource Color.TextSecondary}"/>
    <SolidColorBrush x:Key="Brush.TextMuted" Color="{StaticResource Color.TextMuted}"/>
    <SolidColorBrush x:Key="Brush.TextPlaceholder" Color="{StaticResource Color.TextPlaceholder}"/>
    <SolidColorBrush x:Key="Brush.Border" Color="{StaticResource Color.Border}"/>
    <SolidColorBrush x:Key="Brush.BorderSoft" Color="{StaticResource Color.BorderSoft}"/>
    <SolidColorBrush x:Key="Brush.Surface" Color="{StaticResource Color.Surface}"/>
    <SolidColorBrush x:Key="Brush.SurfaceAlt" Color="{StaticResource Color.SurfaceAlt}"/>
    <SolidColorBrush x:Key="Brush.SurfaceMuted" Color="{StaticResource Color.SurfaceMuted}"/>
    <SolidColorBrush x:Key="Brush.Background" Color="{StaticResource Color.Background}"/>
    <SolidColorBrush x:Key="Brush.HeaderBackground" Color="{StaticResource Color.HeaderBackground}"/>
    <SolidColorBrush x:Key="Brush.HeaderText" Color="{StaticResource Color.HeaderText}"/>

    <!-- 圆角 / 边距尺度 -->
    <CornerRadius x:Key="Radius.Card">10</CornerRadius>
    <CornerRadius x:Key="Radius.Pill">12</CornerRadius>
    <CornerRadius x:Key="Radius.Chip">3</CornerRadius>
    <Thickness x:Key="Padding.Card">16</Thickness>
    <Thickness x:Key="Padding.Toolbar">12,8</Thickness>
    <Thickness x:Key="Margin.SectionGap">0,0,0,12</Thickness>

</ResourceDictionary>
